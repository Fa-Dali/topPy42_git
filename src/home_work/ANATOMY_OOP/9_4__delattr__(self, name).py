# 9_4__delattr__(self, name).py

'''
Представь, что у тебя есть ящик с игрушками, и каждая игрушка обозначена своим
названием. Однажды ты захочешь убрать одну из игрушек, но мама сказала, что
есть особые игрушки, которые нельзя выбрасывать.

Именно здесь и пригодится метод `__delattr__(self, name)` — это особая
команда, которая помогает определить, какие игрушки (атрибуты) можно выбросить,
а какие — оставить навсегда.

🎯 **Принцип работы**:

Когда ты удаляешь атрибут (например, с помощью оператора `del obj.attribute`),
Python первым делом ищет метод `__delattr__`. Если такой метод присутствует,
он управляет процессом удаления, решая, разрешено ли удаление или нет.

Пример простого класса с использованием `__delattr__`:
'''
class ToyBox:
    def __delattr__(self, name):
        if name == 'favorite_toy':
            print("Любимую игрушку удалить нельзя!")
        else:
            object.__delattr__(self, name)

box = ToyBox()
box.favorite_toy = "Медвежонок"
box.extra_toy = "Машинка"

del box.extra_toy  # Удалилось нормально
try:
    del box.favorite_toy  # Попытка удалить любимую игрушку
except Exception as e:
    pass
'''
## 👶 Детали объяснения для ребенка:

Представь, что у тебя есть игрушечный домик с мебелью, и в нём живёт кукла. Ты 
решаешь убраться и начать выкидывать лишнюю мебель. Но родители сказали, что 
кровать и шкаф — самые главные предметы, и их трогать нельзя.

Когда ты попытаешься удалить атрибуты с помощью оператора `del`, Python 
сначала проверяет, есть ли метод `__delattr__`. Если метод есть, он вмешивается
и решает, удалять атрибут или нет.

В нашем примере:
- `extra_toy` — обычная игрушка, её можно спокойно удалить.
- `favorite_toy` — важная игрушка, её трогать нельзя.

Если попытаться удалить важный атрибут, сработает защита и ничего 
не произойдёт. Так обеспечивается сохранность ценных вещей.

## 🔧 Практический совет:
Будь внимателен при использовании метода `__delattr__`. 
Всегда проверяй, есть ли атрибут, прежде чем пытаться его удалить, 
чтобы избежать ошибок.

Используй этот метод, когда нужно защитить важнейшие части данных, 
как ценные сокровища, которые нельзя терять.

===========================================
Особенности, хитрости и нюансы работы с этим.
=============================================

Метод `__delattr__(self, name)` — это специальный метод в Python, который 
позволяет перехватывать операцию удаления атрибутов объекта. Хотя он кажется 
простым, есть немало особенностей и нюансов, знание которых поможет 
использовать его максимально эффективно и безопасно.

## 📌 Особенности и нюансы работы с `__delattr__`

### 1. Механизм вызова

Python вызывает метод `__delattr__`, когда вы используете инструкцию 
`del obj.attr` или `delattr(obj, 'attr')`. 
Прежде чем операция удаления выполнится, интерпретатор ищет этот метод 
в классе объекта. Если метод присутствует, он берёт на себя ответственность 
за удаление указанного атрибута.

#### Пример простейшего использования:
'''
class MyClass:
    def __delattr__(self, name):
        print(f"Пытаюсь удалить атрибут {name}")
        object.__delattr__(self, name)

obj = MyClass()
obj.attr = 42
del obj.attr  # Результат: "Пытаюсь удалить атрибут attr"
'''
### 2. Контроль удаления

Самый полезный аспект метода `__delattr__` — это возможность предотвратить 
удаление важных атрибутов. Вы можете запрограммировать защиту от удаления 
конкретных атрибутов.

#### Пример защиты атрибута:
'''
class ProtectedAttr:
    def __delattr__(self, name):
        if name == 'important_attr':
            raise AttributeError("Удаление важного атрибута запрещено!")
        object.__delattr__(self, name)

pa = ProtectedAttr()
pa.important_attr = "Данные"
try:
    del pa.important_attr  # Произойдет исключение
except AttributeError as e:
    print(e)
'''
### 3. Необходимость вызова `object.__delattr__(self, name)`

Важно отметить, что, если вы собираетесь позволять удаление атрибута, вы 
должны вызвать встроенный метод `object.__delattr__(self, name)`. Иначе 
атрибут останется на месте, несмотря на выполнение инструкции `del`.

#### Неправильный пример:
'''
class BrokenDeletion:
    def __delattr__(self, name):
        print(f"Атрибут {name} удалён.")  # Сообщение выводится, но фактического удаления нет

bd = BrokenDeletion()
bd.some_attr = 123
del bd.some_attr  # Сообщение "some_attr удалён." появляется, но атрибут остаётся!
print(bd.some_attr)  # Всё равно доступно!
'''
### 4. Возможности модификации поведения при удалении

Кроме простого разрешения или отказа в удалении, вы можете интегрировать 
логику очистки или архивации при удалении атрибута. Например, можно перемещать 
удалённый атрибут в отдельный архив, если это необходимо.

#### Пример переноса удалённого атрибута в архив:
'''
class ArchivingAttrs:
    archive = {}  # Архив удалённых атрибутов

    def __delattr__(self, name):
        if hasattr(self, name):
            self.archive[name] = getattr(self, name)  # Копируем атрибут в архив
        object.__delattr__(self, name)

aa = ArchivingAttrs()
aa.data = "Some important data"
del aa.data  # Данные удаляются, но сохраняются в архиве
print(aa.archive)  # Содержит удалённую информацию
'''
### 5. Взаимодействие с другими специальными методами

Метод `__delattr__` тесно связан с методами `__setattr__` и `__getattr__`. 
Их совместное использование позволяет создавать высокоуровневые механизмы 
защиты и управления атрибутами.

Например, можно предусмотреть триаду методов для полного контроля 
над атрибутами:

- `__setattr__` устанавливает атрибуты.
- `__getattr__` перехватывает обращения к отсутствующим атрибутам.
- `__delattr__` контролирует удаление атрибутов.

#### Пример комплексного взаимодействия:
'''
class FullControl:
    def __setattr__(self, name, value):
        print(f"Установлен атрибут {name}")
        object.__setattr__(self, name, value)

    def __getattr__(self, name):
        print(f"Обращение к отсутствующему атрибуту {name}")
        return None

    def __delattr__(self, name):
        print(f"Атрибут {name} удалён")
        object.__delattr__(self, name)

fc = FullControl()
fc.test = 123  # Устанавливаем атрибут
print(fc.test)  # Читаем атрибут
del fc.test  # Удаляем атрибут
'''
## 📌 Тонкости и практическая польза

### 📌 Тонкость №1: Понимание работы цепочки наследования

Если класс наследует атрибуты от другого класса, `__delattr__` воздействует 
только на атрибуты дочернего класса. Родительские атрибуты останутся 
незатронутыми, пока не выполнен вызов `object.__delattr__(self, name)`.

### 📌 Тонкость №2: Искусственное нарушение доступности атрибутов

Иногда необходимо скрыть или искусственно удалить атрибуты, чтобы они 
выглядели как "недоступные". Например, можно отключить весь доступ к 
какому-то атрибуту путём постоянной выдачи исключения в `__delattr__`.

### 📌 Тонкость №3: Производительность

Частое использование методов `__setattr__`, `__getattr__` и `__delattr__` 
может снизить производительность, так как они вызывают дополнительную логику 
при доступе к атрибутам. Применяйте их осмотрительно и учитывайте влияние на 
скорость выполнения программы.

## 🔑 Итог

Метод `__delattr__` позволяет контролировать процесс удаления атрибутов, 
создавая удобные механизмы защиты, ведения архива или реализации особых 
алгоритмов обработки данных. Грамотное использование этого метода сделает 
ваше программное обеспечение более надежным и удобным для дальнейшего развития.

==============================================
ДЛЯ БИЗНЕСА
==============================================

Метод `__delattr__` представляет собой эффективный инструмент для 
программирования и может принести ощутимую пользу бизнесу, предлагая широкий 
спектр возможностей для защиты данных, оптимизации процессов и автоматизации 
различных функций. Рассмотрим несколько случаев, когда этот метод может 
пригодиться в повседневной деятельности компании.

## 📌 Практическое применение в бизнесе

### 📌 **№1. Защита важной информации**

В компаниях часто возникают ситуации, когда определённая информация, 
такая как финансовые отчёты, клиентские базы или производственные планы, 
не должна подвергаться случайному или намеренному удалению. 
С помощью метода `__delattr__` можно предотвратить уничтожение важнейших 
данных.

Пример:
'''
class ImportantDocument:
    def __delattr__(self, name):
        if name == 'content':
            raise AttributeError("Документ важен и не может быть удалён!")
        object.__delattr__(self, name)

doc = ImportantDocument()
doc.content = "Конфиденциальная информация"
try:
    del doc.content  # Удаление запрещено
except AttributeError as e:
    print(e)
'''
Польза для бизнеса: предотвращает потерю важных документов и повышает 
информационную безопасность.

---

### 📌 **№2. Управление версиями и резервное копирование**

Иногда удаление одного параметра означает утрату всей необходимой информации. 
В таких случаях можно применить стратегию автоматического сохранения удалённой 
информации в специальном архиве или журнале изменений.

Пример:
'''
class ArchiveStorage:
    deleted_attrs = {}

    def __delattr__(self, name):
        if hasattr(self, name):
            self.deleted_attrs[name] = getattr(self, name)
        object.__delattr__(self, name)

store = ArchiveStorage()
store.project_data = "Проект X"
del store.project_data  # Проект хранится в архиве
print(store.deleted_attrs)  # {'project_data': 'Проект X'}
'''
Польза для бизнеса: восстановление утраченной информации, 
уменьшение риска потери данных.

---

### 📌 **№3. Контроль удалений для производственной линии**

Производственный цех часто сталкивается с ситуациями, когда внесение 
изменений в спецификации или технологические карты должно происходить 
только с одобрения руководства. Метод `__delattr__` позволяет блокировать 
удаление важных технологических параметров, что способствует повышению 
точности производственного процесса.

Пример:
'''
class TechSpecifications:
    def __delattr__(self, name):
        if name == 'machine_settings':
            raise AttributeError("Настройки машины установлены руководством и не могут быть удалены.")
        object.__delattr__(self, name)

spec = TechSpecifications()
spec.machine_settings = "Режимы обработки стали"
try:
    del spec.machine_settings  # Удаление запрещено
except AttributeError as e:
    print(e)
'''
Польза для бизнеса: повышение качества выпускаемой продукции и снижение риска 
аварий на производстве.

### 📌 **№4. Автоматизация рабочих процессов**

Автоматизированные рабочие процессы требуют тщательной координации всех шагов. 
Если один шаг неожиданно удаляется, рабочий процесс нарушается. Использование 
`__delattr__` позволяет избежать непредвиденного прерывания автоматизированных 
процессов.

Пример:
'''
class WorkflowStep:
    def __delattr__(self, name):
        if name == 'step_sequence':
            raise AttributeError("Последовательность этапов рабочего процесса не может быть нарушена.")
        object.__delattr__(self, name)

process = WorkflowStep()
process.step_sequence = [1, 2, 3]
try:
    del process.step_sequence  # Последовательность шага защищена
except AttributeError as e:
    print(e)
'''
Польза для бизнеса: увеличение продуктивности и улучшение автоматизации 
производственных процессов.

### 📌 **№5. Организация централизованного хранилища данных**

Компаниям важно иметь центральное хранилище данных, откуда извлекаются нужные 
сведения. Для предотвращения утраты нужной информации можно использовать метод 
`__delattr__`, который будет блокировать удаление центральных записей.

Пример:
'''
class CentralRepository:
    def __delattr__(self, name):
        if name == 'central_data':
            raise AttributeError("Центральные данные не подлежат удалению.")
        object.__delattr__(self, name)

repo = CentralRepository()
repo.central_data = "Совокупность данных о сотрудниках"
try:
    del repo.central_data  # Удаление заблокировано
except AttributeError as e:
    print(e)
'''
Польза для бизнеса: гарантия сохранности важных данных, повышение надежности 
информационной системы.

## 📌 Итог

Метод `__delattr__` способен внести значительный вклад в успешное 
функционирование бизнеса, обеспечивая следующие преимущества:

- Надежная защита критической информации.
- Эффективное резервное копирование и хранение исторических данных.
- Увеличение производительности за счёт автоматизации и снижения риска ошибок.
- Более точное следование процедурам и инструкциям в производственной среде.

Таким образом, грамотное внедрение метода `__delattr__` в повседневные 
процессы способно укрепить позиции компании, улучшить качество услуг и 
снизить операционные расходы.

==============================================
ДЛЯ МЕБЕЛЬНОГО ПРОИЗВОДСТВА
==============================================

Ниже приведены десять наглядных примеров использования метода `__delattr__` 
специально для мебельного производства. Каждый пример демонстрирует конкретную 
выгоду для бизнеса в области контроля, безопасности и удобства работы.

## 📌 Пример №1: Запрет удаления спецификаций моделей
Спецификация модели мебели должна оставаться доступной и актуальной. 
Удаление спецификации чревато потерей важных характеристик изделия.
'''
class ModelSpecification:
    def __delattr__(self, name):
        if name == 'specification':
            raise AttributeError("Спецификацию модели удалять нельзя.")
        object.__delattr__(self, name)

model = ModelSpecification()
model.specification = {
    'материал': 'массив дуба',
    'размеры': 'ширина 120см, глубина 60см, высота 80см'
}
try:
    del model.specification  # Спецификация защищена
except AttributeError as e:
    print(e)
'''
## 📌 Пример №2: Резервирование удалённых чертежей
Файлы с чертежами зачастую требуются повторно. Перед удалением файл 
отправляется в архив, чтобы при необходимости восстановить чертеж.
'''
class DrawingArchive:
    deleted_drawings = {}

    def __delattr__(self, name):
        if name == 'drawing_file':
            self.deleted_drawings[name] = getattr(self, name)
        object.__delattr__(self, name)

draw = DrawingArchive()
draw.drawing_file = 'chair_design.dwg'
del draw.drawing_file  # Чертеж попадает в архив
print(draw.deleted_drawings)  # {'drawing_file': 'chair_design.dwg'}
'''
## 📌 Пример №3: Запрет удаления планов раскроя материала
План раскроя определяет оптимальное распределение материалов, и его удаление 
может негативно сказаться на эффективности производства.
'''
class CuttingPlan:
    def __delattr__(self, name):
        if name == 'plan':
            raise AttributeError("План раскроя не может быть удалён.")
        object.__delattr__(self, name)

cut_plan = CuttingPlan()
cut_plan.plan = [
    ('шкаф', 1),
    ('полка', 2),
    ('дверца', 3)
]
try:
    del cut_plan.plan  # План защищён
except AttributeError as e:
    print(e)
'''
## 📌 Пример №4: Заморозка технологического процесса
Технология сборки изделия прописана изначально и не должна изменяться в 
процессе работы. Метод `__delattr__` позволяет заморозить последовательность 
сборки.
'''
class AssemblyProcess:
    def __delattr__(self, name):
        if name == 'steps':
            raise AttributeError("Процесс сборки утвержден и не подлежит изменению.")
        object.__delattr__(self, name)

assembly = AssemblyProcess()
assembly.steps = ['установка каркаса', 'монтаж дверей', 'обивка тканью']
try:
    del assembly.steps  # Процесс защищён
except AttributeError as e:
    print(e)
'''
## 📌 Пример №5: Архив образцов ткани
Образцы тканей, используемые ранее, сохраняют ценность как источник 
вдохновения или референс. Удаление образца недопустимо, но метод `__delattr__` 
позволяет отправить образец в архив.
'''
class FabricSample:
    archived_samples = {}

    def __delattr__(self, name):
        if name == 'sample':
            self.archived_samples[name] = getattr(self, name)
        object.__delattr__(self, name)

fabric = FabricSample()
fabric.sample = 'floral_pattern.jpg'
del fabric.sample  # Образец попал в архив
print(fabric.archived_samples)  # {'sample': 'floral_pattern.jpg'}
'''
## 📌 Пример №6: Ограничение удаления шаблонов моделей
Шаблоны моделей необходимы для воспроизведения аналогичных изделий. 
Удаление шаблона недопустимо, поэтому его можно защитить.
'''
class TemplateModel:
    def __delattr__(self, name):
        if name == 'template':
            raise AttributeError("Шаблон модели не может быть удалён.")
        object.__delattr__(self, name)

tmpl = TemplateModel()
tmpl.template = 'диван_classic.txt'
try:
    del tmpl.template  # Шаблон защищён
except AttributeError as e:
    print(e)
'''
## 📌 Пример №7: Каталог цветов покрытия
Каталог цвета покрытий необходим для подбора подходящей отделки. Удаление
 каталога может помешать выбору оптимального варианта окраски.
'''
class ColorCatalog:
    def __delattr__(self, name):
        if name == 'colors':
            raise AttributeError("Каталог цветов покрытий не может быть удалён.")
        object.__delattr__(self, name)

catalog = ColorCatalog()
catalog.colors = ['белый', 'бежевый', 'серый']
try:
    del catalog.colors  # Каталог защищён
except AttributeError as e:
    print(e)
'''
## 📌 Пример №8: История покупок материалов
История закупок важна для оценки эффективности закупки и планирования будущих 
закупок. Метод `__delattr__` позволяет отправлять покупки в архив, если 
потребуется освободить память.
'''
class PurchaseHistory:
    purchase_archive = {}

    def __delattr__(self, name):
        if name == 'purchase':
            self.purchase_archive[name] = getattr(self, name)
        object.__delattr__(self, name)

ph = PurchaseHistory()
ph.purchase = {'дата': '2023-06-01', 'сумма': 10000}
del ph.purchase  # Покупка уходит в архив
print(ph.purchase_archive)  # {'purchase': {...}}
'''
## 📌 Пример №9: Сохранение эскизов дизайнов
Эскизы дизайнерских решений служат источником идей и ссылок на будущее.
 Даже если дизайн устареет, его можно временно переместить в архив.
'''
class DesignSketch:
    sketch_archive = {}

    def __delattr__(self, name):
        if name == 'sketch':
            self.sketch_archive[name] = getattr(self, name)
        object.__delattr__(self, name)

design = DesignSketch()
design.sketch = 'modern_table.png'
del design.sketch  # Эскиз перемещается в архив
print(design.sketch_archive)  # {'sketch': 'modern_table.png'}
'''
## 📌 Пример №10: Блокировка удаления прайс-листа
Прайс-лист формируется на основании текущих рыночных цен и закупочных 
стоимостей. Удаление прайс-листа грозит нарушением расчетов себестоимости и 
прибыли.
'''
class PriceList:
    def __delattr__(self, name):
        if name == 'prices':
            raise AttributeError("Прайс-лист не может быть удалён.")
        object.__delattr__(self, name)

pricing = PriceList()
pricing.prices = {'стул': 1000, 'стол': 5000}
try:
    del pricing.prices  # Прайс-лист защищён
except AttributeError as e:
    print(e)
'''
## 📌 Заключение
Метод `__delattr__` идеально подходит для предприятий, занимающихся мебельным
производством, поскольку позволяет гибко контролировать удаление данных, 
обеспечивая целостность и защиту жизненно важных активов. Его правильное 
использование укрепляет управленческий контроль, уменьшает риск человеческих 
ошибок и помогает систематизировать процессы.

'''