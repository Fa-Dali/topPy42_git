# что такое свойства (`properties`) и зачем они нужны.

## Представь, что твой персонаж в игре может менять свою одежду и размер обуви...
'''
Но вот проблема: твоя игра не хочет позволять персонажу надеть обувь
размером меньше нуля (это было бы странно!). Именно тут на помощь
приходят свойства — удобный инструмент, который защищает твои данные от
неправильных изменений.

Посмотрим простой пример.
### Как это выглядит на примере роста человечка?

Допустим, у тебя есть персонаж-человечек, которому можно задать рост.
Мы хотим убедиться, что рост всегда положительный (не ниже нуля),
иначе это нелепо выглядело бы в игре.

#### Правило игры:
Рост персонажа не может быть отрицательным значением.
Иначе игрок увидит странную ситуацию: «Рост равен минус 1 метр!» 😅

Чтобы защититься от такой ситуации, мы используем свойства.
'''

### Объяснение по шагам 👇
'''
1. **Создание скрытого поля**  
   Внутри класса мы создаем специальное закрытое поле (`_рост`), 
   которое прячется от игрока. Игроки видят только красивое название 
   `рост`, а реально значение сохраняется именно там, куда они 
   не смогут залезть напрямую.

2. **Получение значения**  
   Через специальную команду `@property` мы говорим программе: 
   «Если кто-нибудь спросит рост персонажа, 
   покажи ему наше скрытое значение (_рост)!»

3. **Установка значения**  
   Команда `@рост.setter` следит за изменениями. 
   Прежде чем принять новую высоту, она проверяет её: 
   если высота положительная — всё окей, записываем её. 
   Если нет — кричим об ошибке.
'''


### Давай посмотрим на примере 🎮

class Человечек:
    # Скрытый атрибут для хранения реального роста
    def __init__(self, рост):
        self._рост = рост  # начальное значение сразу присваивается нормально

    # Получаем рост спокойно
    @property
    def рост(self):
        return self._рост

    # Устанавливаем рост осторожно
    @рост.setter
    def рост(self, значение):
        if значение > 0:
            self._рост = значение
        else:
            raise ValueError("Рост не может быть отрицательным.")

    # Создание персонажа


герой = Человечек(170)

# Попробуем установить нормальный рост
герой.рост = 180
print(герой.рост)  # Всё отлично! Рост установлен: 180 см

# А попробуем поставить неверный рост (-10)
герой.рост = -10  # Тут выскочит ошибка: ValueError:
# Рост не может быть отрицательным.


### Зачем вообще это нужно?
'''
Это важно, потому что теперь мы уверены, что никто случайно 
или специально не сможет испортить данные твоего персонажа. 
Все изменения проходят строгую проверку, и приложение остаётся 
защищённым от глупостей.

Таким образом, свойства — это заботливые стражники, охраняющие порядок 
и безопасность в твоей программе.
'''


### ПРИМЕР ДЛЯ БИЗНЕСА
### Бизнес-задача: расчет зарплаты сотрудника 💼📌
class Сотрудник:
    def __init__(self, фамилия, оклад, премия=0):
        self._фамилия = фамилия
        self._оклад = оклад
        self._премия = премия

    # Свойство фамилии сотрудника (можно только читать)
    @property
    def фамилия(self):
        return self._фамилия

    # Свойство оклада сотрудника (может меняться, но только положительно)
    @property
    def оклад(self):
        return self._оклад

    @оклад.setter
    def оклад(self, новая_сумма):
        if новая_сумма >= 0:
            self._оклад = новая_сумма
        else:
            raise ValueError("Оклад не может быть отрицательным.")

    # Свойство премии (также контролируем установку положительного значения)
    @property
    def премия(self):
        return self._премия

    @премия.setter
    def премия(self, новая_сумма):
        if новая_сумма >= 0:
            self._премия = новая_сумма
        else:
            raise ValueError("Премия не может быть отрицательной.")

    # Метод расчета итоговой зарплаты (включает премию)
    def рассчитатьЗарплату(self):
        return self._оклад + self._премия


'''
Теперь предположим, что бухгалтер нашей фирмы решает обновить зарплату 
сотруднику и начислить премию:
'''
# Создаем сотрудника Ивана Ивановича
ivan = Сотрудник("Иванов", 50000)

# Посмотрим его текущую зарплату
print(f"Оклад Ивана Ивановича: {ivan.оклад}")  # Оклад Ивана Ивановича: 50000

# Повышаем оклад сотруднику (все проходит гладко)
ivan.оклад = 55000
print(f"Новый оклад: {ivan.оклад}")  # Новый оклад: 55000

# Добавляем премию за хорошую работу
ivan.премия = 10000
print(f"Новая премия: {ivan.премия}")  # Новая премия: 10000

# Рассчитываем итоговую заработную плату
зарплата = ivan.рассчитатьЗарплату()
print(f"Итоговая зарплата Ивана Ивановича: {зарплата}")  # Итоговая зарплата
# Ивана Ивановича: 65000
# ==================================================
'''
про СВОЙСТВА (property)

Представь себе дом с дверью и окном. Ты хочешь увидеть комнату внутри дома, 
но дверь закрыта, и ключ потерян. 

Что же делать?

Тут приходит идея — сделать специальное окошко! Через окно мы можем видеть, 
что находится внутри комнаты, и даже иногда управлять вещами там, не 
открывая двери!

**Свойства (`@property`) в Python** работают примерно так же.

## 📌 Что такое свойства?

Обычно в классах мы храним данные («атрибуты») и методы, которые управляют 
    этими данными. Но представь, что доступ к данным нужно ограничить — 
    например, запретить кому-то менять важные значения напрямую. Или наоборот —
    позволить изменять значение особым образом.

Вот тут и приходят на помощь **`@property`**. 
Это специальные инструменты, 
        позволяющие создать окна-доступы к нашим внутренним данным таким 
        образом, чтобы контролировать чтение и изменение значений.

## 👶 Объясняем простыми словами

### Свойство работает так:
Допустим, у тебя есть класс `Человек`, и у каждого человека есть возраст 
(`age`). Если разрешить каждому устанавливать любой возраст прямо в 
переменную — появится риск случайных ошибок. Например, кто-нибудь случайно 
установит отрицательное число или совсем другое значение типа строки.

Но свойство позволяет решить проблему следующим образом:

'''
class Человек:
    def __init__(self):
        self._возраст = 0  # Обратите внимание на 
        # подчёркивание перед именем атрибута.

    @property
    def возраст(self):
        return self._возраст

    @возраст.setter
    def возраст(self, value):
        if isinstance(value, int) and value >= 0:
            self._возраст = value
        else:
            print("Возраст должен быть положительным числом!")
'''

Теперь попробуем посмотреть, как это работает:
'''
человек = Человек()
print(человек.возраст)  # Получаем значение возраста.
человек.возраст = 25  # Устанавливаем новое значение.
print(человек.возраст)
# Попробуем установить некорректное значение:
человек.возраст = -10
# Программа выведет предупреждение: Возраст должен быть положительным числом!
'''

Видишь, как удобно? Мы контролируем доступ к возрасту: можем читать и 
записывать его безопасным способом.

## ⚙️ Зачем нужны свойства?
- **Безопасность**: Позволяют проверять правильность вводимых данных.
- **Простота чтения/изменения**: Можно обращаться к данным, будто это 
обычные поля, но на самом деле выполняются скрытые проверки и логика.
- **Скрытие деталей реализации**: Изменение внутренней структуры класса
не повлияет на внешний интерфейс программы.

Таким образом, **`@property`** — это волшебное окно, которое помогает 
лучше защитить наши внутренние данные от нежелательных изменений и 
обеспечивает удобный доступ к ним!

# ==========================================

Давай рассмотрим применение свойств (`@property`) в бизнес-контексте 
на примере реальных ситуаций.

## 🔧 Примеры из реальной жизни

### ✅ Пример №1: ============= Магазин обуви онлайн ===============

У магазина есть каталог товаров, и цена товара зависит от разных факторов 
(скидка, акция, стоимость доставки).

Создаем класс `Обувь`, в котором нам важно обеспечить контроль над ценой:
'''
class Обувь:
    def __init__(self, название, базовая_цена):
        self._название = название
        self._базовая_цена = базовая_цена
        self._скидка = 0

    @property
    def цена(self):
        """Вычисляем итоговую цену с учётом скидки."""
        return self._базовая_цена * (1 - self._скидка / 100)

    @price.setter
    def цена(self, новая_ценабез_скидки):
        """Устанавливаем новую базовую цену."""
        if isinstance(новая_ценабез_скидки, float) or isinstance(
                новая_ценабез_скидки, int):
            self._базовая_цена = новая_ценабез_скидки
        else:
            raise ValueError("Цена должна быть числовой.")

    @property
    def скидка(self):
        return self._скидка

    @скидка.setter
    def скидка(self, процент_скидки):
        if isinstance(процент_скидки, int) and 0 <= процент_скидки <= 100:
            self._скидка = процент_скидки
        else:
            raise ValueError("Скидка должна быть целым числом между 0 и 100%")


# Используем свойства:
туфельки = Обувь('Балетки', 1500)
туфельки.скидка = 10
print(f'Итоговая цена туфелек: {туфельки.цена}')
'''

Преимущества:
- Безопасность данных: нельзя установить неверную скидку или 
неправильную цену.
- Удобство вычисления цены: финальная сумма рассчитывается автоматически.

---
### ✅ Пример №2: ======= Управление складскими запасами ============

Предположим, компания занимается производством электроники, и ей нужно следить 
за количеством продукции на складе.

Класс `Продукт`:
'''
class Продукт:
    def __init__(self, наименование, начальное_количество=0):
        self._наименование = наименование
        self._количество = начальное_количество

    @property
    def количество(self):
        return self._количество

    @количество.setter
    def количество(self, new_amount):
        if isinstance(new_amount, int) and new_amount >= 0:
            self._количество = new_amount
        else:
            raise ValueError("Количество должно быть неотрицательным числом.")

    def списать_со_склада(self, amount):
        if amount > self._количество:
            raise ValueError("Недостаточно продукта на складе.")
        self._количество -= amount

# Использование классов:
телефон = Продукт('Смартфон', 100)
телефон.списать_со_склада(10)
print(f"Телефонов осталось: {телефон.количество}")
'''

Зачем тут свойство?
- Ограничивает возможность установки недопустимого количества товара 
        (например, отрицательного числа).
- Упрощает управление складом благодаря контролю за изменениями запасов.

---
### ✅ Пример №3: ========= Финансовое приложение банка ==========

Рассмотрим класс клиента банка, где нам важно поддерживать конфиденциальность 
личных данных и обеспечивать безопасность изменения баланса счета.

Класс `КлиентБанк`:
'''
class КлиентБанк:
    def __init__(self, имя, баланс=0):
        self._имя = имя
        self._баланс = баланс

    @property
    def баланс(self):
        return f"{self._имя}, ваш текущий баланс: {self._баланс}"

    @balance.setter
    def balance(self, new_balance):
        if isinstance(new_balance, float) or isinstance(new_balance, int):
            self._баланс = new_balance
        else:
            raise TypeError("Баланс должен быть числом.")

# Работаем с клиентом:
клиент = КлиентБанк('Иван Петров')
клиент.balance = 10000
print(client.баланс)
'''

Почему именно свойство?
- Оно защищает клиентов от неправильного ввода денежных сумм 
        (некорректная валюта, символы и т.п.).
- Автоматически формирует понятное сообщение о балансе клиенту.

## 💡 Заключение

Использование свойств делает код устойчивее, защищённее и удобнее для 
понимания и поддержки. Они помогают бизнесу эффективно защищать ключевые 
данные, минимизировать риски ошибок и обеспечивают прозрачность операций 
с информацией.
# ============================================

Использование декоратора `@property` предотвращает ряд распространённых 
ошибок и повышает качество кода. Вот наиболее частые проблемы, от которых 
спасают свойства:

## 🛠️ Какие ошибки предотвращаются свойствами?

### 1. Неправильные типы данных
Ошибочно присвоив объекту неправильное значение 
(например, строку вместо числа), можно вызвать сбой приложения. 
Свойства позволяют легко проверить входящее значение и отклонять 
неподходящие типы.

Пример:
'''
class Автомобиль:
    def __init__(self, марка, пробег):
        self._марка = марка
        self._пробег = пробег

    @property
    def пробег(self):
        return self._пробег

    @пробег.setter
    def пробег(self, новый_пробег):
        if not isinstance(новый_пробег, int) or новый_пробег < 0:
            raise ValueError("Пробег автомобиля должен быть положительным числом.")
        self._пробег = новый_пробег
'''

Без свойств программист мог бы случайно записать пробеги строками или 
отрицательными значениями, вызвав сбои расчётов и отчетности.

### 2. Логические ошибки
Иногда бывает, что одно свойство связано с другим. Свойства позволяют 
учитывать зависимости между полями и избегать противоречий.

Например, товар имеет скидку, зависящую от текущего уровня лояльности 
покупателя:
'''
class Товар:
    def __init__(self, название, цена):
        self._название = название
        self._цена = цена
        self._скидка = 0

    @property
    def цена(self):
        return self._цена * (1 - self._скидка / 100)

    @scount.setter
    def скидка(self, new_discount):
        if new_discount < 0 or new_discount > 100:
            raise ValueError("Скидка должна быть в диапазоне от 0 до 100%.")
        self._скидка = new_discount
'''

Это предотвращает ситуации, когда покупатель получит абсурдную скидку 
больше 100%, что могло привести к убыткам.


### 3. Несанкционированное изменение важных данных
Прямой доступ к приватным полям класса открывает потенциальную угрозу 
безопасности. Свойства контролируют любые попытки изменить критичные значения.

Пример:
'''
class Пользователь:
    def __init__(self, email, пароль):
        self._email = email
        self._пароль = пароль

    @property
    def пароль(self):
        return '******'

    @password.setter
    def password(self, new_password):
        # Проверяем сложность пароля
        if len(new_password) < 8:
            raise ValueError("Пароль слишком короткий.")
        self._пароль = hashlib.sha256(new_password.encode()).hexdigest()  # хэшируем пароль
'''

Здесь свойство гарантирует, что никто не сможет легко прочитать настоящий 
пароль и одновременно заставляет соблюдать правила сложности паролей.

---

### 4. Недопущение дублирования и избыточности
Часто некоторые данные зависят друг от друга, и повторное задание одних и тех 
же значений вручную увеличивает вероятность ошибки. Свойства позволяют 
динамически рассчитывать зависимые величины и избежать проблем.

Пример:
'''
class Заказ:
    def __init__(self, товары):
        self._товары = товары

    @property
    def общая_стоимость(self):
        return sum([item['цена'] for item in self._товары])
'''

Здесь нет риска забыть обновить общую сумму заказа вручную каждый раз, когда 
меняется список товаров.

## 🗃️ Итоги

Использование свойств значительно снижает вероятность появления ряда типичных 
ошибок:
- Некорректные типы данных.
- Невалидные диапазоны значений.
- Нарушение целостности связанных полей.
- Несанкционированный доступ к чувствительной информации.
- Повторное обновление данных вручную.

Эти преимущества делают классы более надёжными и удобными в работе, 
особенно в крупных проектах и системах управления бизнесом.

'''
