'''
__new__(cls[, ...]): ...Создает новый экземпляр класса.

Представь, что ты хочешь построить дом. Сначала ты думаешь: какой дом нужен?
Большой или маленький? Красивый или обычный? После того, как решил, что
именно строить, ты берёшь чертеж и инструкции (класс) и начинаешь
строительство дома (создание объекта).

Теперь представь, что до строительства дома ты можешь выбрать фундамент —
бетонный, деревянный или металлический. То есть выбираешь самое первое
основание, на котором будешь возводить здание. Именно это и делает метод
__new__

🔸 **Что делает метод __new__?**
__new__ — это первый этап создания объекта. До того, как появится сам
объект (дом), он определяет, как именно объект будет создан. Затем передает
эстафету другому методу — __init__, который займётся отделкой интерьера
и обстановкой.

🔹 **Почему важно использовать __new__?**

Обычно этот метод используют редко, потому что Python прекрасно справляется
с созданием объектов автоматически. Но иногда хочется внести тонкости:
например, задать специальное правило для создания объектов или предотвратить
создание нескольких одинаковых объектов одновременно.

📌 **Простой пример:**

Давай сделаем игрушку — робота. Ты захочешь собрать много разных моделей
роботов, но заранее решишь, что робот обязательно должен иметь имя.
Так вот, в методе `__new__` мы сможем гарантировать, что у каждого робота
обязательно будет уникальное имя, даже если случайно забудем присвоить
его позже.

'''
class Robot:
    def __new__(cls, name):
        robot = object.__new__(cls)  # Создаем пустой объект
        robot.name = name  # Назначаем имя роботу
        return robot

    def greet(self):
        print(f"Привет, меня зовут {self.name}.")

robot1 = Robot("Робби")
robot1.greet()  # Результат: Привет, меня зовут Робби.
'''
### 🎈 Почему этот пример хороший?

- Мы создали робота с именем.
- Имя присваивается сразу, как только робот создается.
- Даже если забыть присвоить имя в будущем, робот уже получил его при рождении.

Теперь ты знаешь, зачем нужен метод `__new__` и как он устроен!
'''
# ПРИМЕР ДЛЯ БИЗНЕСА
'''
Давайте поговорим о методе `__new__` в терминах бизнеса, чтобы стало понятно, 
какую ценность он несет в бизнес-проектах и приложениях.

### 🏷️ Зачем бизнесу метод `__new__`?
Предположим, ваша компания занимается производством уникальных продуктов 
или услуг, и каждое изделие должно соответствовать строгим стандартам 
качества и спецификациям. Вы хотели бы гарантировать, что каждая единица 
продукции соответствует установленным требованиям еще до начала производства.

Именно такую гарантию даёт метод `__new__` в Python.

        Допустим, наша компания производит смартфоны, и каждому устройству 
        должна быть присвоена уникальная серийная маркировка, состоящая ровно 
        из 15 символов. Задача состоит в том, чтобы убедиться, что при 
        создании каждого смартфона серия всегда формируется верно и 
        соответствует требуемому формату.

📱 Код примера:
'''
class Smartphone:
    def __new__(cls, serial_number):
        """
        Гарантирует, что серийный номер точно соответствует требованиям,
        прежде чем смартфон будет создан.
        """
        if len(serial_number) != 15:
            raise ValueError("Серийный номер должен состоять ровно из 15 символов.")
        return object.__new__(cls)

    def __init__(self, serial_number):
        """Инициализация смартфона"""
        self.serial_number = serial_number

    def show_serial(self):
        """Отображает серийный номер телефона."""
        print(f"Серийный номер: {self.serial_number}")

# Тестируем создание смартфонов
try:
    phone1 = Smartphone("SN-ABCDEF-GHIJKL")
    phone1.show_serial()  # Серийный номер правильный, выводится успешно.

    phone2 = Smartphone("SN-WRONG-SIZE")  # Будет ошибка, т.к. длина неверная.
    phone2.show_serial()

except ValueError as error:
    print(error)
'''

### 🛠️ **Процесс создания продукта**

Допустим, вы запускаете производство автомобилей. Прежде чем начнется сборка 
автомобиля (экземпляра класса), вы проверяете, соответствуют ли комплектующие 
необходимым техническим характеристикам (спецификации класса). Только 
убедившись, что все компоненты идеальны, вы приступаете к сборке авто.

🚗 Шаг 1: Определение требований к автомобилю
Допустим, у нас есть три компонента, которые обязаны присутствовать 
в автомобиле:
- Двигатель (объем не меньше 1 литра),
- Колеса (количество равняется четырем),
- Кузов (соответствует ГОСТам по толщине металла).
Если хотя бы одно требование нарушено, машина не должна быть собрана вообще.
'''
class Car:
    def __new__(cls, engine_volume, wheel_count, body_thickness):
        """
        Проверяет технические характеристики деталей перед сборкой автомобиля.
        """
        if engine_volume < 1.0:
            raise ValueError("Объем двигателя недостаточен.")
        elif wheel_count != 4:
            raise ValueError("Количество колес не соответствует норме.")
        elif body_thickness < 0.5:
            raise ValueError("Толщина кузова недостаточная.")

        # Все тесты пройдены, начинаем собирать автомобиль
        return object.__new__(cls)

    def __init__(self, engine_volume, wheel_count, body_thickness):
        """
        Сохраняет параметры автомобиля после успешной проверки.
        """
        self.engine_volume = engine_volume
        self.wheel_count = wheel_count
        self.body_thickness = body_thickness

    def start_engine(self):
        print(f"Двигатель объемом {self.engine_volume} литров запущен.")

# Тестируем создание автомобилей
try:
    # Автомобиль с правильными характеристиками
    car1 = Car(engine_volume=1.6, wheel_count=4, body_thickness=0.8)
    car1.start_engine()  # Должно пройти успешно.

    # Автомобиль с нарушением стандартов (слишком тонкий кузов)
    car2 = Car(engine_volume=1.6, wheel_count=4, body_thickness=0.3)
    car2.start_engine()  # Должно вывести ошибку.

except ValueError as err:
    print(err)
'''
🖥️ Бизнес-ценность:
Такая схема решает две важные задачи:
- Контроль качества: Автомобили собираются только из проверенных компонентов, 
что существенно сокращает риск выпуска брака.
- Оптимизация расходов: Поскольку проблемные изделия отсекаются раньше, 
экономятся средства на исправления дефектов и доработки уже собранных 
автомобилей.

В программировании этот этап эквивалентен созданию объекта с помощью метода 
`__new__`. Таким образом, `__new__` становится первым этапом контроля качества 
при производстве программного продукта.

### ✅ **Защита данных и безопасность**

Рассмотрим компанию, работающую с конфиденциальной информацией клиентов. 
Чтобы избежать утечек данных, каждое обращение к данным должно проходить 
проверку. Например, метод `__new__` может обеспечить, что при создании 
объекта (клиента, документа или транзакции) проводится обязательная проверка 
прав доступа или ограничений конфиденциальности.

📋 Постановка задачи:
    У нас есть медицинский центр, который хранит личные данные пациентов. 
    Пациенты имеют различные уровни доступа: врач может видеть всю медицинскую 
    карту целиком, медсестра — только некоторую часть, а администратор видит 
    минимум необходимой информации.

Наша цель — создать систему, где попытка прочитать запись медицинской 
    карты возможна только в том случае, если уровень доступа сотрудника 
    достаточный.
    
'''
class MedicalRecord:
    def __new__(cls, patient_data, user_role):
        """
        Проверьте права доступа перед созданием медкарты.
        """
        allowed_roles = {
            "doctor": ["full_access"],
            "nurse": ["partial_access"],
            "administrator": []
        }

        # Проверьте право доступа пользователя
        if user_role not in allowed_roles:
            raise PermissionError("Недостаточно прав для просмотра "
                                  "медицинских записей.")

        # Врач имеет полный доступ, остальные роли получают частичную информацию
        if allowed_roles[user_role][0] == "full_access":
            record = object.__new__(cls)
            record.data = patient_data
        else:
            # Ограничиваем информацию для ролей с меньшими правами
            restricted_data = {"patient_name": patient_data["patient_name"]}
            record = object.__new__(cls)
            record.data = restricted_data

        return record

    def __init__(self, patient_data, user_role):
        """
        Конструктор для инициализации записей.
        - Конструктор __init__ оставлен пустым, потому что вся нужная логика
            помещена в метод __new__.
        - Весь процесс строится вокруг одного этапа проверки и формирования
            объекта, поэтому дополнительный шаг инициализации не требуется.
        """
        pass  # Ничего не делаем тут, так как вся логика находится в __new__

    def view_record(self):
        """
        Просмотр доступной части медкарты.
        """
        print(f"Ваш уровень доступа: {self.data}")

# Пример использования
try:
    doctor_record = MedicalRecord(
        {"patient_name": "Алексей Иванов",
         "medical_history": "Гипертония"},
        "doctor")
    nurse_record = MedicalRecord(
        {"patient_name": "Олег Петров",
         "medical_history": "Диабет"},
        "nurse")
    admin_record = MedicalRecord(
        {"patient_name": "Анна Смирнова",
         "medical_history": "Аллергия"},
        "administrator")

    doctor_record.view_record()  # Врач увидит полную информацию
    nurse_record.view_record()   # Медсестра получит ограниченную информацию
    admin_record.view_record()   # Администратор увидит минимальный объем данных

except PermissionError as e:
    print(e)
'''

### ⚙️ **Применение в бизнесе**

Рассмотрим реальный сценарий: у вашей фирмы имеется приложение для 
бронирования авиабилетов. Каждое бронирование должно сопровождаться 
уникальной идентификационной информацией пассажира. Используя метод 
`__new__`, вы сможете установить обязательное условие: каждый билет 
должен содержать уникальный номер клиента и действительный паспорт. 
Без этих условий система откажется формировать бронь.

'''
class Booking:
    def __new__(cls, passenger_id, passport_number):
        if len(passenger_id) != 9 or len(passport_number) != 10:
            raise ValueError("Неверный формат данных.")
        booking = object.__new__(cls)
        booking.passenger_id = passenger_id
        booking.passport_number = passport_number
        return booking

    def display_booking_details(self):
        print(f"Бронирование выполнено. Пассажир ID: {self.passenger_id}, "
              f"Номер паспорта: {self.passport_number}")

booking = Booking("123456789", "AB12345678")
booking.display_booking_details()
'''
### 💡 **Заключение**

Метод `__new__` подобен первой линии защиты и фильтрации данных. 
Его задача — подтвердить, что создаваемый объект имеет необходимые 
характеристики и строго соблюдает стандарты качества и требования бизнеса. 
В результате это повышает надежность приложений, минимизирует риски сбоев 
и снижает вероятность критичных ошибок.
'''