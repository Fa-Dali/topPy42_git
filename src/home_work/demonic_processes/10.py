# пример — классический шаблон многопоточного приложения на Python

'''
Приведенный вами пример — классический шаблон многопоточного
приложения на Python с использованием встроенного модуля `threading`.
Он хорошо подходит для ситуаций, когда нужно разделить сложную задачу
на несколько независимых потоков выполнения, чтобы повысить скорость
обработки данных или снизить время отклика программы.

### Где и как применять этот код:

Данный шаблон подойдет для любых задач, где требуется распределение
нагрузки между несколькими потоками. Рассмотрим несколько практических
случаев применения:

---

### 🗄️ **Пример 1: Система резервирования билетов**

Представьте сайт продажи билетов на концерты или мероприятия.
Одновременно многие пользователи хотят купить билеты.
Вам нужно распределить поступающие запросы между несколькими потоками,
чтобы обеспечить высокую пропускную способность и быстродействие.

#### Решение:
Создать несколько потоков, каждый из которых будет отвечать за
обработку отдельной очереди запросов на покупку билета. Например,
один поток может заниматься регистрацией покупателей, другой —
подтверждением покупки, третий — выпуском электронных билетов.

---

### 🖼️ **Пример 2: Веб-сервер с поддержкой нескольких соединений**

Допустим, вы разрабатываете веб-сервер, который должен поддерживать
множество входящих подключений одновременно. Каждое соединение требует
отдельного потока для обработки запросов, чтения данных и отправки
ответов.

#### Решение:
Организуйте пул потоков, где каждый поток отвечает за отдельное
подключение. Например, один поток слушает входящие соединения, а
остальные занимаются обработкой запросов от клиентов.

---

### 🛢️ **Пример 3: Сбор данных с датчиков**

Предположим, у вас есть сеть датчиков, установленных в разных местах
города, которые собирают экологические показатели (температура воздуха,
уровень шума, загрязненность воды). Данные собираются непрерывно, и их
нужно сохранить в базу данных.

#### Решение:
Один поток будет считывать показания с датчика, другой — сохранять
полученные данные в базу, третий — отправлять отчеты в облачное
хранилище.

---

### 📈 **Пример 4: Автоматизация тестирования**

Допустим, вы хотите автоматизировать тестирование веб-сайта, проводя
серию тестов параллельно. Тестирование включает регистрацию
пользователей, вход в аккаунт, создание записей и удаление данных.

#### Решение:
Разделите различные типы тестов на независимые потоки. Один поток
регистрирует новых пользователей, другой загружает файлы, третий
проверяет работоспособность страниц.

---

### 📱 **Пример 5: Распределённая сборка статистической отчетности**

Нужно собрать финансовую отчетность компании за месяц. Каждый филиал
генерирует свои собственные отчёты, которые необходимо объединить в
общий документ.

#### Решение:
Можно задействовать несколько потоков, каждый из которых обрабатывает
отчёты одного филиала, формирует промежуточные итоги и передает их
главному потоку для финальной сборки отчета.

---

### Как адаптировать исходный код под любую из задач:

Допустим, мы адаптируем пример под задачу сбора показаний с датчиков
окружающей среды:

'''
import threading
import time

class SensorReader(threading.Thread):
    def __init__(self, sensor_id, name):
        threading.Thread.__init__(self)
        self.sensor_id = sensor_id
        self.name = name

    def run(self):
        print(f"Запуск сенсора {self.name}")
        measure_temperature(self.sensor_id)
        print(f"Выход из сенсора {self.name}")

def measure_temperature(sensor_id):
    temperature = get_sensor_reading(sensor_id)
    save_to_database(sensor_id, temperature)

def get_sensor_reading(sensor_id):
    return round((sensor_id * 10) / 2 + 20, 2)  # эмуляция показаний температуры

def save_to_database(sensor_id, temp):
    print(f"Данные сохранены: Сенсор {sensor_id}, температура {temp}C")

# Создание потоков для датчиков
sensors = [SensorReader(i, f"Sensor-{i}") for i in range(1, 4)]  # Датчики с ID 1, 2, 3

# Запуск потоков
for s in sensors:
    s.start()

# Ожидание завершения всех потоков
for s in sensors:
    s.join()

print("Сбор данных завершен.")
'''

### Итог:

Исходный шаблон, который вы привели, универсален и легко адаптируется 
практически под любые задачи, требующие распределения нагрузки между 
несколькими потоками. Главное — правильно выбрать точки разделения 
задач и организовать взаимодействие между ними.
'''