'''
## Метод `threading.enumerate()` объясняется простым языком 👇

✨ **Детский пример:**
Представьте себе группу ребят, играющих в футбол на площадке. Кто-то
бегает с мячом, кто-то защищает ворота, кто-то отдыхает на скамейке
запасных. Ты хочешь узнать, кто именно сейчас играет на поле. Тогда
ты можешь перечислить всех игроков и их роли прямо сейчас.

То же самое делает метод `threading.enumerate()`: он выдаёт список
всех работающих в данный момент потоков (игроков на футбольном поле)
вместе с информацией о том, что они делают.

Простой детский пример:
'''
import threading

# Игроки-функции
def run_around():
    print("Игрок бежит вокруг поля!")

def guard_goal():
    print("Игрок защищает ворота!")

# Тренер создает команду
player1 = threading.Thread(target=run_around)
player2 = threading.Thread(target=guard_goal)

# Пускаем игру
player1.start()
player2.start()

# Список всех действующих игроков
players_on_field = threading.enumerate()

# Показываем игроков на поле
for player in players_on_field:
    print(player.name + ": участвует в игре!")
'''

Что мы делаем:
- Два игрока играют свои роли: один бегает, другой охраняет ворота.
- Метод `enumerate()` показывает, кто сейчас активно участвует в игре.

***
👩‍💻 **Примеры для бизнеса:**  
Представьте крупную компанию, где происходят сотни транзакций одновременно: 
оплата счетов, обновление баз данных, рассылка уведомлений клиентам. 
Очень удобно периодически получать отчёт обо всех происходящих действиях 
и анализировать производительность.

### Бизнес-пример №1: Онлайн-школа  
У школы десятки уроков идут одновременно, студенты проходят тесты, 
смотрят лекции, общаются с преподавателем. Через метод `enumerate()` 
школа видит, какие уроки находятся в процессе выполнения и следит за 
загрузкой серверов.

Код:
'''
import threading
import time

# Преподаватель ведёт урок
def conduct_class(class_name):
    print(f"Урок '{class_name}' начался")
    time.sleep(2)  # имитируем длительность урока
    print(f"Урок '{class_name}' завершился")

# Учебные группы
classes = ['История искусств', 'Программирование', 'Английский']

# Запуск уроков
threads = []
for class_name in classes:
    lesson = threading.Thread(target=conduct_class, args=(class_name,))
    threads.append(lesson)
    lesson.start()

# Получаем список текущих уроков
running_lessons = threading.enumerate()

# Отображаем активные уроки
for lesson in running_lessons:
    print(f"Активный урок: {lesson.name}")

# Дождёмся окончания всех уроков
for thread in threads:
    thread.join()

print("Все уроки завершены!")
'''

### Бизнес-пример №2: Логистика грузоперевозок  
Логистический оператор управляет большим числом транспортных маршрутов 
одновременно. Используя `enumerate()`, компания контролирует все 
текущие маршруты грузов и выявляет заторы или задержанные рейсы.

Код:
'''
import threading
import random
import time

# Груз перевозится по маршруту
def transport_cargo(route):
    print(f"Груз движется по маршруту '{route}'")
    delay_time = random.randrange(1, 5)
    time.sleep(delay_time)  # имитируем транспортировку груза
    print(f"Маршрут '{route}' завершён")

# Маршруты перевозок
routes = ['Москва-Санкт-Петербург', 'Екатеринбург-Нижний Новгород', 'Владивосток-Хабаровск']

# Организация перевозки
transport_threads = []
for route in routes:
    cargo_transportation = threading.Thread(target=transport_cargo, args=(route,))
    transport_threads.append(cargo_transportation)
    cargo_transportation.start()

# Мониторинг текущих рейсов
active_routes = threading.enumerate()

# Отображение списка активных рейсов
for route in active_routes:
    print(f"Действующий рейс: {route.name}")

# Ожидаем завершение всех перевозок
for thread in transport_threads:
    thread.join()

print("Все грузы доставлены!")
'''

### Итоги:

Метод `threading.enumerate()` позволяет получить перечень всех текущих 
потоков и понять, что именно сейчас происходит в вашей программе. 
Будь то детская игра, образовательная платформа или логистическая 
служба, умение видеть текущие активности помогает лучше управлять 
ресурсами и избегать перегрузок.
'''