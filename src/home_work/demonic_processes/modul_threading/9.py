'''
## Метод `setName()` в классе `Thread`

### Что делает метод `setName()`?

Метод `setName()` позволяет установить новое имя для существующего
потока. Имя потока — это уникальная строка, предназначенная для
идентификации потока среди других. Чаще всего это полезно для целей
отладки и мониторинга, поскольку оно помогает визуально отличить один
поток от другого.

### Значимость метода `setName()`:

Правильно подобранное имя потока значительно упрощает чтение и отладку
многопоточного кода. Программисты могут назначать осмысленные имена,
соответствующие назначению потоков, что снижает вероятность ошибок и
затруднений при разработке и эксплуатации программного продукта.

### Пример использования метода `setName()`

'''
import threading
import time

class MyThread(threading.Thread):
    def __init__(self, name=None):
        super().__init__()
        if name is not None:
            self.setName(name)

    def run(self):
        print(f"Запущенный поток: {self.getName()}")
        time.sleep(2)  # Симулируем выполнение задачи
        print(f"Завершился поток: {self.getName()}")

# Создаем и запускаем несколько потоков
thread1 = MyThread(name="Первый поток")
thread2 = MyThread(name="Второй поток")

thread1.start()
thread2.start()

# Ждем завершения потоков
thread1.join()
thread2.join()

print("Все потоки завершены.")
'''

### Пояснение к примеру:
- Мы создали класс `MyThread`, наследующий стандартную библиотеку `Thread`.
- Конструктор класса принимает аргумент `name`, который передается 
    в метод `setName()`, устанавливая имя потока.
- При запуске потоков метод `run()` выводит на экран имя потока, 
    демонстрируя, что имя установлено правильно.

### Особенности метода `setName()`:
- Имя потока можно менять динамически в ходе выполнения программы.
- Установка имени удобна для понимания логики работы многопоточных 
    приложений.
- Имя потока не обязано быть уникальным, но уникальность способствует 
    лучшей читаемости кода.

Таким образом, метод `setName()` предоставляет эффективный механизм для 
улучшения качества сопровождения и поддержки многопоточных приложений, 
улучшая удобство разработки и отладки.

========================================================================

### Детский пример с объяснением 🦾
Представь, что у тебя есть любимые игрушки — машинки, которые умеют 
ездить сами по себе. У каждой машинки есть своё имя, чтобы ты мог 
отличать их друг от друга. Одна машинка называется "Спринтер", другая — 
"Турбонавт", третья — "Газелька". Эти имена помогают тебе говорить 
друзьям, какую именно машинку сейчас водишь.

Компьютеры такие же: когда у них есть несколько маленьких работников 
(называемых "потоками"), важно давать каждому своему имя, чтобы легко 
ориентироваться, кто что делает.

Сейчас мы научим наши маленькие машинкам объявлять своё имя при 
движении. Попробуем написать такую программку на Python:

'''
import threading
import time

# Машинка ездит сама
def drive_car(car_name):
    print(f"Машинка {car_name} стартанула!")
    time.sleep(2)  # Едем пару секунд
    print(f"Машинка {car_name} прибыла на финиш!")

# Заводим три машинки
cars = ["Спринтер", "Турбонавт", "Газелька"]

# Создаем и запускаем машинки
for car in cars:
    car_thread = threading.Thread(target=drive_car, args=(car,))
    car_thread.setName(car)  # Назвали нашу машинку
    car_thread.start()

# Ждём финиша всех машинок
for _ in cars:
    time.sleep(1)

print("Заезд завершён!")
'''

### Что здесь происходит:
- Мы заводим три машинки с именами "Спринтер", "Турбонавт" и "Газелька".
- Каждая машинка двигается по трассе (симулируется паузой в 2 секунды).
- Перед началом движения мы называем каждую машинку 
    с помощью метода `setName()`.
- Машинки сообщают своё имя при старте и финише гонки.

Результатом станет вывод в консоль сообщений с именами машинок, 
участвующих в гонке.

Таким образом, метод `setName()` помогает организовать порядок и 
облегчить контроль за поведением потоков, подобно тому, как ты помнишь 
имена своих любимых игрушек.

========================================================================

## Примеры использования метода `setName()` в бизнесе 🔥

### Пример №1: Транспортная компания по доставке товаров 🚚

Транспортная компания доставляет товары круглосуточно. Каждое 
транспортное средство (грузовик, фургон) представляет собой отдельный 
поток. Каждому потоку необходимо назначить уникальное имя, чтобы 
централизованно отслеживать, какая машина в данный момент совершает 
доставку.

'''
import threading
import time

# Поток, осуществляющий доставку
def make_delivery(truck_name):
    print(f"Автомобиль {truck_name} начал доставку.")
    time.sleep(3)  # Имитация времени доставки
    print(f"Автомобиль {truck_name} завершил доставку.")

# Автомобили транспортной компании
vehicles = ["Грузовик 1", "Фургон 2", "Автобус 3"]

# Параллельно осуществляем доставку всеми автомобилями
threads = []

for vehicle in vehicles:
    truck_thread = threading.Thread(target=make_delivery, args=(vehicle,))
    truck_thread.name = vehicle  # ☝️ Новый способ установки имени потока
    threads.append(truck_thread)
    truck_thread.start()

# Отслеживаем автомобили
for thread in threads:
    print(f"Следим за автомобилем {thread.name}.")  # Обращаемся напрямую к атрибуту name

# Ждём завершения всех автомобилей
for thread in threads:
    thread.join()

print("Все доставки завершены.")
'''

### Пример №2: Банковское приложение для денежных переводов 🤝

В банке пользователи регулярно осуществляют денежные переводы. Каждую 
транзакцию выполняет отдельный поток. Необходимо знать, какая именно 
транзакция выполняется в данный момент, чтобы обеспечить безопасность и 
надежность операций.

'''
import threading
import time

# Поток, выполняющий денежный перевод
def process_transfer(transfer_id):
    print(f"Начинается обработка перевода №{transfer_id}")
    time.sleep(2)  # Время обработки перевода
    print(f"Перевод №{transfer_id} завершен.")

# Переводы клиентов
transfers = [1001, 1002, 1003]

# Осуществляем переводы параллельно
threads = []

for transfer in transfers:
    transfer_thread = threading.Thread(target=process_transfer, args=(transfer,))
    transfer_thread.name = f"Transfer_{transfer}"  # Установили имя потока напрямую
    threads.append(transfer_thread)
    transfer_thread.start()

# Следим за ходом переводов
for thread in threads:
    print(f"Наблюдаем за переводом {thread.name}.")  # Обращаемся напрямую к атрибуту name

# Ждём завершения всех переводов
for thread in threads:
    thread.join()

print("Все переводы завершены.")
'''

### Пример №3: Медицинская клиника для диагностики заболеваний 🧑‍⚕️

Медицинская клиника производит массовую диагностику пациентов. Каждое 
обследование проводится отдельным врачом (потоком). Врачам нужны 
индивидуальные имена, чтобы врачи могли представить отчёты и 
отслеживать результаты обследований.

'''
import threading
import time

# Диагностика пациента
def examine_patient(doctor_name):
    print(f"Доктор {doctor_name} начал осмотр пациента.")
    time.sleep(3)  # Временной отрезок осмотра
    print(f"Доктор {doctor_name} завершил осмотр.")

# Врачи клиники
doctors = ["Иван Петров", "Анна Смирнова", "Сергей Иванов"]

# Проводим обследования параллельно
threads = []

for doctor in doctors:
    examination_thread = threading.Thread(target=examine_patient, args=(doctor,))
    examination_thread.name = doctor  # ☝️ Теперь используем прямой доступ к атрибуту name
    threads.append(examination_thread)
    examination_thread.start()

# Следим за врачами
for thread in threads:
    print(f"Осматривает пациент доктор {thread.name}.")

# Ждём завершения осмотров
for thread in threads:
    thread.join()

print("Все пациенты осмотрены.")
'''

### Итог:
Метод `setName()` важен для правильного наименования потоков, что 
облегчает разработку и поддержку масштабируемых решений в бизнесе. 
Приведённые примеры иллюстрируют, насколько полезным оказывается 
установление уникальных имён потоков для решения повседневных задач 
компаний разного профиля.
'''