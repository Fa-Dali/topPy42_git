'''
## Метод `is_alive()` в классе `Thread`

### Что значит метод `is_alive()`?
Метод `is_alive()` используется для проверки статуса потока.
Он возвращает булево значение:

- **True**, если поток всё ещё активен (выполняется),
- **False**, если поток завершил свою работу.

### Зачем нужен метод `is_alive()`?
Иногда нужно удостовериться, что определенный поток ещё работает,
прежде чем предпринимать какие-либо действия. Особенно это актуально в
случаях, когда важно синхронизировать работу нескольких потоков или
принять решение о дальнейших шагах в зависимости от их состояния.

### Пример использования метода `is_alive()`
'''
import threading
import time

class MyThread(threading.Thread):
    def __init__(self, message):
        super().__init__()
        self.message = message

    def run(self):
        print(f"Стартовал поток: {self.message}")
        time.sleep(3)  # Пауза для симуляции длинной операции
        print(f"Завершается поток: {self.message}")

# Создаем и запускаем поток
thread = MyThread("Рабочий поток")
thread.start()

# Периодически проверяем активность потока
while thread.is_alive():
    print("Поток еще активен.")
    time.sleep(1)

print("Поток завершил работу.")
'''

### Пояснение к примеру:
- Мы создали поток, выполняющий простую операцию с небольшой задержкой.
- После запуска потока проверяем его состояние с помощью метода `is_alive()`.
- Цикл продолжается, пока поток активен, и выводит соответствующее 
    сообщение каждые секунду.
- Как только поток заканчивает свою работу, цикл прерывается, и 
    программа переходит к следующему действию.

### Другие особенности метода `is_alive()`:
- Метод не влияет на работу потока, он просто проверяет состояние.
- Полезен для мониторинга состояний сложных систем, состоящих из 
    множества потоков.
- Может использоваться в комбинации с другими методами вроде `join()` 
    для точной настройки взаимодействия потоков.

Таким образом, метод `is_alive()` помогает разработчику легко 
определить, активен ли в данный момент определённый поток, что 
критично для правильной синхронизации и эффективного управления
потоками в приложениях.

=======================================================================

### Простой пример для ребёнка с объяснением 🧐

Представь, что у тебя есть любимый робот, который умеет танцевать. 
Однажды ты решил научить его двум новым движениям: первое движение — 
кружиться, второе — хлопать руками. Но ты хочешь узнать, танцует ли 
робот в данный момент, чтобы вовремя остановить танец.

В мире компьютеров это делается с помощью метода `is_alive()`. 
Давай разберемся, как это устроено:

'''
import threading
import time

# Наш веселый робот-танцор
def dance_robot():
    print("Робот начал крутиться и хлопать!")
    time.sleep(5)  # Танец длится 5 секунд
    print("Робот перестал танцевать.")

# Создаем танцовщика (поток)
dancer = threading.Thread(target=dance_robot)

# Говорим роботу начать танец
dancer.start()

# Время от времени интересуемся, танцует ли робот
while dancer.is_alive():
    print("Робот все еще танцует!")
    time.sleep(1)  # Ждем секунду, чтобы снова проверить

print("Танец закончился.")
'''

### Что происходит в нашем сценарии:
1. Робот начинает танцевать (метод `start()`).
2. Программа постоянно проверяет, танцует ли робот, 
    с помощью метода `is_alive()`.
3. Пока робот танцует, мы видим сообщение "Робот все еще танцует!".
4. Когда танец заканчивается, программа прекращает проверку и сообщает: 
    "Танец закончился."

### Похоже на жизнь!

Представь, что робот — это помощник, которого ты попросил приготовить 
чай. Ты периодически заглядываешь на кухню и говоришь: "Ты еще готовишь 
чай?" ("is_alive()"). Когда чай готов, ты узнаешь это и приглашаешь 
друзей пить чай.

Метод `is_alive()` — это волшебный вопрос, который помогает узнать, чем 
занят твой помощник прямо сейчас! 🎶

=======================================================================

## Примеры использования метода `is_alive()` в бизнес-процессах 🔥

### Пример №1: Интернет-магазин с обработкой заказов

Интернет-магазин принимает огромное количество заказов, и для быстрого 
обслуживания клиентов каждый заказ обрабатывается отдельным потоком. 
Менеджеру важно знать, когда конкретные заказы завершены, чтобы обновить 
статистику продаж.
'''
import threading
import time

# Обработка заказа
def process_order(order_number):
    print(f"Начинается обработка заказа №{order_number}")
    time.sleep(3)  # Процесс обработки заказа
    print(f"Закончена обработка заказа №{order_number}")

# Запускаем обработку нескольких заказов
orders = [101, 102, 103]
threads = []

for order in orders:
    thread = threading.Thread(target=process_order, args=(order,))
    threads.append(thread)
    thread.start()

# Следим за статусом обработки заказов
for thread in threads:
    while thread.is_alive():
        print(f"Обработка заказа продолжается... (заказ №{order})")
        time.sleep(1)

print("Все заказы обработаны.")
'''

### Пример №2: Банковская система с мониторингом платежей

Крупный банк осуществляет платежи сотен тысяч клиентов одновременно. 
Нужно постоянно мониторить, какое количество платежных запросов еще 
выполняется, чтобы распределять нагрузку между серверами и сообщать 
пользователям точные сроки обработки переводов.
'''
import threading
import time

# Процесс перевода денег
def transfer_money(account_from, account_to):
    print(f"Перевод начат: {account_from} → {account_to}")
    time.sleep(2)  # Имитация времени обработки
    print(f"Перевод завершен: {account_from} → {account_to}")

# Текущие переводы
transfers = [(12345, 67890), (54321, 98765)]
threads = []

for transfer in transfers:
    thread = threading.Thread(target=transfer_money, args=transfer)
    threads.append(thread)
    thread.start()

# Мониторинг переводов
while any(thread.is_alive() for thread in threads):
    print("Ожидайте завершения переводов...")
    time.sleep(1)

print("Все переводы выполнены.")
'''

### Пример №3: Сервис доставки еды с контролем сроков доставки
Сервис доставки еды отправляет заказы курьеру, и важно контролировать, 
сколько заказов в данный момент находится в процессе доставки. Эта 
информация полезна для информирования клиентов и оптимизации маршрутов.

'''
import threading
import time

# Процесс доставки заказа
def deliver_food(order_id):
    print(f"Курьер выезжает с заказом №{order_id}")
    time.sleep(3)  # Время доставки
    print(f"Заказ №{order_id} доставлен")

# Активные заказы
orders = [1001, 1002, 1003]
threads = []

for order in orders:
    thread = threading.Thread(target=deliver_food, args=(order,))
    threads.append(thread)
    thread.start()

# Контроль доставки
while any(thread.is_alive() for thread in threads):
    print("Некоторые заказы еще в пути...")
    time.sleep(1)

print("Все заказы доставлены.")
'''

### Выводы:
Метод `is_alive()` незаменим для предприятий, где работают большие
объемы параллельных задач. Это удобный инструмент для постоянного 
мониторинга активности потоков, чтобы решать задачи эффективнее и 
оперативнее реагировать на ситуации.
'''