'''
## Метод `threading.active_count()` объясняет простым языком 👇

🎯 **Для детей:**
Представьте, что у вас есть большая игрушечная железная дорога, и
поезда едут по разным путям сразу. Если спросить: "Сколько поездов
сейчас едет?" — вот именно это делает метод `active_count()`: он
показывает, сколько сейчас активных поездов-тредов работает параллельно! 😊

Вот маленький примерчик-кодовочка, чтобы было понятно даже ребёнку:

'''
import threading

# Представляем поезд №1
def train_1():
    print("Поезд №1 отправился!")

# Представляем поезд №2
def train_2():
    print("Поезд №2 тоже отправляется!")

# Запускаем первый поезд
train_thread_1 = threading.Thread(target=train_1)
train_thread_1.start()

# Проверяем, сколько всего поездов запущено
print(f"Сейчас активны {threading.active_count()} поезда.")

# Теперь запустим второй поезд
train_thread_2 = threading.Thread(target=train_2)
train_thread_2.start()

# Опять проверим число поездов
print(f"Теперь активны {threading.active_count()} поезда.")
'''

Что мы тут сделали?
- Сначала создали один поезд, запустили его и посмотрели, сколько 
    поездов в пути (получилось одно активное задание).
- Потом добавили ещё один поезд и снова проверили счётчик — теперь 
    там стало два поезда!

***

📌 **Для бизнеса:**  
Допустим, ваша компания обрабатывает большое количество заказов 
клиентов онлайн. У каждого заказа своя маленькая задача: проверить 
наличие товара, подготовить доставку, выставить счёт и отправить 
уведомление клиенту. Каждое такое маленькое дело выполняется отдельным 
процессом ("потоком"), чтобы клиенты ждали меньше и всё работало 
быстрее.  

Так вот, иногда полезно знать, сколько заданий прямо сейчас активно 
выполняются, чтобы понимать нагрузку системы. Именно для этого служит 
метод `active_count()`: он покажет вам точное количество активных 
процессов в вашем приложении.

Теперь покажу бизнес-пример:

'''
import threading
from time import sleep

# Моделирование обработки клиентского заказа
def process_order(order_id):
    print(f"Принят заказ №{order_id}. Обрабатываем...")
    sleep(2)  # Имитация долгой операции (например, проверка склада)
    print(f"Заказ №{order_id} обработан успешно!")

orders = ["A123", "B456", "C789"]

# Используем потоки для параллельной обработки всех заказов
threads = []
for order in orders:
    thread = threading.Thread(target=process_order, args=(order,))
    threads.append(thread)
    thread.start()

# Посмотрим, сколько заказов сейчас в обработке
while any(t.is_alive() for t in threads):
    active_threads = threading.active_count()
    print(f"В данный момент обслуживаются {active_threads} заказа(-ов)")
    sleep(1)

print("Все заказы обработаны!")
'''

Здесь мы моделируем обработку трёх заказов одновременно разными 
потоками. После запуска каждого потока смотрим, сколько заказов 
находится в активной обработке, пока все задания не завершатся.

Таким образом, этот метод помогает следить за нагрузкой и 
оптимизировать работу вашего сервиса!
'''