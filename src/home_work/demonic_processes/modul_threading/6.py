'''
## Метод `join()` в классе `Thread`

### Определение метода `join()`
Метод `join()` применяется к объектам класса `Thread` и предназначен для
ожидания завершения работы потока. Когда вы вызываете `join()` для
определенного потока, основной поток останавливается и ждет, пока
указанный поток закончит свою работу.

Также метод `join()` поддерживает необязательный аргумент — временной
интервал (в секундах), который ограничивает максимальное время ожидания
завершения потока. Если указанный промежуток истекает раньше завершения
потока, метод возвращается, а поток продолжает свою работу независимо
от основного потока.

### Пример использования метода `join()`
'''
import threading
import time

class MyThread(threading.Thread):
    def __init__(self, message):
        super().__init__()
        self.message = message

    def run(self):
        print(f"Начало работы потока: {self.message}")
        time.sleep(3)  # Пауза для симуляции длительных операций
        print(f"Конец работы потока: {self.message}")

# Создаем и запускаем поток
thread = MyThread("Test thread")
thread.start()

# Ждем завершения потока с таймаутом в 2 секунды
try:
    thread.join(timeout=2)
except Exception as e:
    print(e)

# Проверяем, жив ли поток после join()
if thread.is_alive():
    print("Поток еще не закончил работу.")
else:
    print("Поток завершил работу.")

print("Основной поток продолжил выполнение.")
'''

### Подробности:
- Метод `join()` блокирует выполнение главного потока до тех пор, пока 
    целевой поток не завершится.
- Аргумент `timeout` ограничивает продолжительность ожидания (если указан). 
    Если поток не успевает завершить работу за указанное время, метод 
    вернется, и программа продолжит работу дальше.
- После возврата из `join()` вы можете проверить состояние потока, 
    используя метод `is_alive()`, чтобы убедиться, действительно ли 
    поток завершил свою работу.

### Дополнительные важные моменты:
- Вызывать `join()` желательно после запуска потока методом `start()`, 
    иначе он сработает некорректно.
- Можно также вызывать `join()` без аргументов, и тогда программа будет 
    ждать неограниченное время до завершения потока.

Таким образом, метод `join()` — мощный инструмент для координации работы 
потоков, обеспечивающий аккуратное управление временем выполнения программы.

=======================================================================

### Просто о методе `join()` для ребенка 🚀

Представь, что ты строишь домик из кубиков вместе с другом. Пока твой 
друг собирает крышу, ты собираешь стены. Но твоя мама сказала: 
"Дом готов, когда крыша сделана!"

Поэтому ты ждёшь, пока друг соберёт крышу, прежде чем сказать маме, 
что дом закончен. Ведь дом считается готовым только тогда, когда обе 
части готовы.

В компьютере похожее происходит с методами `start()` и `join()`. 
Метод `start()` запускает другого помощника (другого "работника") 
собирать что-то важное. А метод `join()` заставляет тебя подождать, 
пока помощник завершит свою работу, прежде чем продолжить программу.

Например, ты пишешь робота, который должен сделать две вещи:
1. Скачать картинку с сайта.
2. Нарисовать рамочку на картинке.

Картинку скачивать долго, а рисовать рамочку быстро. Значит, сначала 
надо скачать картинку, а уж потом нарисовать рамочку.

Вот как это будет выглядеть на языке программирования Python:
'''
import threading
import time

# Работник, который скачивает картинку
def download_picture():
    print("Начинаю скачивать картинку...")
    time.sleep(5)  # Загрузка идет медленно
    print("Картинка скачана!")

# Работник, который рисует рамочку
def draw_frame():
    print("Рисую рамочку...")
    time.sleep(1)  # Рисование рамки быстрое
    print("Рамочка готова!")

# Создаем работника (поток), который займется скачиванием
downloader = threading.Thread(target=download_picture)

# Начинаем скачивание картинки
downloader.start()

# Ждем, пока картинка скачается
downloader.join()

# Теперь начинаем рисовать рамочку
draw_frame()

print("Готово! Картинка с рамочкой создана.")
'''

### Что здесь произошло:
1. Робот сказал помощнику "скачай картинку" (`start()`).
2. Потом робот подождал, пока помощник сделает это (`join()`).
3. Только после загрузки началась вторая работа — рисунок рамки.

Получается, `join()` заставил робота дождаться окончания первой важной 
задачи (загрузки картинки), прежде чем приступить ко второй (рисованию рамки).

Итак, метод `join()` позволяет программе остановиться и терпеливо 
подождать, пока важный помощник завершит свою работу. Так же, как ты 
ждешь, пока друг построит крышу дома, прежде чем пригласить маму 
оценить готовый домик! ✅
'''