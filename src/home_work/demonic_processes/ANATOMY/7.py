'''
Давай рассмотрим понятие семафоров (`Semaphore`) подробно и простыми
словами, чтобы разобраться, как они работают и зачем нужны.

## 🎛️ **Что такое Semaphore (семафор)?**

Семафор — это специальный механизм, предназначенный для регулирования
доступа к какому-либо ресурсу, ограничивая максимальное количество
потоков, которые могут одновременно работать с этим ресурсом.

Представь железнодорожный переезд с закрытыми воротами. Светофор
регулирует движение поездов, пропуская их строго по очереди, чтобы
избежать столкновений. Семафор в программировании работает примерно
так же: он контролирует, сколько потоков может одновременно получить
доступ к ресурсу.

---

## 🌐 **Как это устроено?**

В Python семафоры представлены классом `Semaphore` из модуля
`threading`. Семафор обладает внутренним счётчиком, который показывает,
сколько потоков разрешено работать с ресурсом одновременно.

- **Инициация семафора**: задаётся начальное значение
    (например, `Semaphore(2)` — допускает максимум два потока одновременно).
- **Захват семафора (`acquire()`)**: поток пытается занять доступ к ресурсу.
    Если свободен, пропускает поток вперёд; если занято, ждёт освобождения.
- **Освобождение семафора (`release()`)**: поток освобождает ресурс, давая
    другим потокам шанс получить доступ.

---
## 📌 **Типичный сценарий использования семафора**
Представь, что у тебя ресторан с двумя кассирами. Одновременно обслуживание
ведут только два кассира, а третья касса простаивает. Клиенты выстраиваются
в очередь, и когда освободится место, следующий клиент зайдёт в свободную
кассу.

Семафор здесь действует как регулятор движения клиентов, контролируя,
сколько клиентов могут стоять на обслуживании одновременно.

---
### 📚 **Полный пример использования семафора**
Представим парковку возле ресторана, которая вмещает только два
автомобиля одновременно. Мы создадим два потока автомобилей, желающих
припарковаться, и убедимся, что правила соблюдены.

'''
import threading
import time

# 🚗 Паркинг на 2 машины
parking_semaphore = threading.Semaphore(value=2)

# 🚗 Автомобиль паркуется
def park_car(car_number):
    parking_semaphore.acquire()  # Захват парковки
    print(f"Автомобиль {car_number} занял парковку.")
    time.sleep(3)  # Машина стоит на стоянке 3 секунды
    print(f"Автомобиль {car_number} уехал с парковки.")
    parking_semaphore.release()  # Освобождение парковки

# 🚗 Машины приезжают
cars = [1, 2, 3, 4]

# 🚗 Формируем паркующиеся автомобили
threads = []
for car in cars:
    thread = threading.Thread(target=park_car, args=(car,))
    threads.append(thread)
    thread.start()

# 🚗 Ждём, пока все машины припаркуются и уедут
for thread in threads:
    thread.join()

print("Все машины приехали и уехали, паркинг закрыт.")
'''

---
### 🌾 **Что происходит в этом примере?**
1. **Парковка (`parking_semaphore`)**:
   - Ограничивает количество машин, которые могут находиться на парковке 
        одновременно (до двух машин).
2. **Захват парковки (`acquire()`)**:
   - Автомобили занимают парковку по порядку. Если парковка занята, 
        машина ждёт освобождения.
3. **Освобождение парковки (`release()`)**:
   - Когда автомобиль покидает парковку, он освобождает слот, позволяя 
        следующей машине въехать.

---
### 🚀 **Зачем нужен семафор?**
- **Контроль доступности**: семафор позволяет регулировать количество 
    потоков, одновременно работающих с ресурсом.
- **Исключение перегрузки**: если ресурс ограничен (например, база 
    данных, сервер или оборудование), семафор помогает избежать 
    избыточной нагрузки.
- **Балансировка нагрузки**: обеспечивает равномерное распределение 
    нагрузки между ресурсами.

---
### 📈 **Примеры использования семафоров в реальной жизни**
- Контроль количества одновременных обращений к API сервиса.
- Управление количеством рабочих процессов на серверах.
- Балансировка загрузки оборудования в вычислительных кластерах.

---
### 🎯 **Заключение**
Семафоры — это важный инструмент для регуляции доступа к ресурсам, 
когда нужно ограничивать количество потоков, одновременно работающих с 
этим ресурсом. Их использование помогает избежать перегрузок и улучшает 
стабильность системы.

Надеюсь, теперь идея семафоров стала намного понятнее!

========================================================================

Давай разберём концепцию семафоров на простом и весёлом примере, 
который понравится детям.

### 🌽 **История про кукурузные палочки**
Представь, что у тебя дома есть корзинка с ароматными кукурузными 
палочками. Но палочек всего десять, и сразу съесть их все нельзя, 
иначе заболит живот. Ты и твои друзья договорились, что одновременно 
кушать палочки могут только два человека, чтобы не объесться.

Чтобы контролировать доступ к корзинке, вы решили завести волшебный 
значок — **«семафор»**, который выдаётся тому, кому разрешено брать 
палочки. Всего значков два, поэтому только двое могут лакомиться 
одновременно.

---
### 📌 **Правила игры**
- Если ты хочешь взять палочку, ты просишь волшебный значок.
- Если значок свободен, ты берёшь палочку и ешь её.
- Когда съел, отдаёшь значок обратно, чтобы его мог взять следующий.

---
### 🖥️ **Реализация детской игры на Python**

'''
import threading
import time

# 🌽 Корзинка с палочками (ограниченный ресурс)
corn_snacks = 10

# 🚦 Волшебный значок (семафор)
snack_semaphore = threading.Semaphore(value=2)  # Одновременный доступ для двоих

# 🍿 Процесс еды (один игрок)
def eat_corn_snack(player_name):
    snack_semaphore.acquire()  # Просим значок
    print(f"🍿 {player_name} ест кукурузную палочку!")
    global corn_snacks
    if corn_snacks > 0:
        corn_snacks -= 1
        print(f"🌽 Осталось палочек: {corn_snacks}")
    else:
        print(f"🍿 {player_name} не нашёл палочек, все съедены!")
    time.sleep(2)  # Едим пару секунд
    snack_semaphore.release()  # Возвращаем значок обратно

# 🍿 Игроки (потоки)
players = ["Миша", "Наташа", "Сережа", "Настя"]

# 🍿 Играем (создаём игроков)
threads = []
for player in players:
    thread = threading.Thread(target=eat_corn_snack, args=(player,))
    threads.append(thread)
    thread.start()

# 🍿 Ждём, пока все игроки перекусят
for thread in threads:
    thread.join()

print("Игра закончена, все сытые и довольные!")
'''

---
### 🌾 **Что происходит в этом коде?**
1. **Волшебный значок (`snack_semaphore`)**:
   - Семафор с лимитом в два игрока, контролирующий, сколько человек 
        одновременно могут есть палочки.
2. **Процесс еды (`eat_corn_snack`)**:
   - Игрок просит значок (`acquire()`), съедает палочку, а затем 
        возвращает значок (`release()`).
3. **Ограниченность ресурса**:
   - Палочек всего десять, и они уменьшаются по мере потребления игроками.
4. **Потоки (игроки)**:
   - Каждый игрок — это отдельный поток, стремящийся добраться до 
        палочек и насладиться вкусом.

---
### 🎯 **Заключение**
Семафоры — это замечательный инструмент для контроля доступа к ресурсам, 
особенно когда ресурс ограничен и его нужно равномерно распределять 
между пользователями. Представляя это в игровой форме, мы видим, 
насколько важны такие механизмы для поддержания порядка и избежания 
чрезмерного потребления.

Надеюсь, теперь концепция семафоров стала гораздо понятнее и интереснее!

=======================================================================

Давай перейдём к бизнесу и рассмотрим реальный пример использования 
семафоров в коммерческих условиях.

Представь крупную компанию, которая владеет небольшим автопарком 
служебных автомобилей. Компания решила внедрить систему бронирования 
авто сотрудниками, но хочет ограничить количество работников, которые 
могут бронировать машину одновременно, чтобы избежать перегрузки 
системы и путаницы.

Семафор поможет обеспечить безопасный и контролируемый доступ к сервису 
аренды автомобилей.

---
### 🚗 **Бизнес-пример: аренда корпоративных автомобилей**

'''
import threading
import time
import random

# 🚗 Доступные автомобили
cars = ["Toyota Camry", "Honda Civic", "Ford Focus"]

# 🔐 Лимит на количество сотрудников, бронирующих авто одновременно
booking_semaphore = threading.Semaphore(value=2)  # Двое сотрудников
                                    # могут бронировать одновременно

# 👨‍💼 Сотрудник бронирует автомобиль
def book_car(employee_name):
    booking_semaphore.acquire()  # Запрашиваем разрешение забронировать авто
    print(f"📌 Сотрудник {employee_name} бронирует автомобиль.")
    available_car = random.choice(cars)
    print(f"🚗 {employee_name} выбрал {available_car}.")
    time.sleep(3)  # Симуляция времени бронирования
    print(f"✅ {employee_name} завершил бронирование {available_car}.")
    booking_semaphore.release()  # Освобождаем доступ для других сотрудников

# 👨‍💼 Сотрудники компании
employees = ["Иван Иванов",
             "Ольга Петрова",
             "Дмитрий Сидоров",
             "Екатерина Смирнова"]

# 🚗 Организация бронирования
threads = []
for employee in employees:
    thread = threading.Thread(target=book_car, args=(employee,))
    threads.append(thread)
    thread.start()

# 🚗 Ждём завершения всех бронирований
for thread in threads:
    thread.join()

print("Все сотрудники завершили бронирование автомобилей.")
'''

---
### 📌 **Что происходит в этом примере?**
1. **Система бронирования (`booking_semaphore`)**:
   - Семафор ограничивает количество сотрудников, которые могут 
        одновременно бронировать автомобиль (в данном случае двое).
2. **Процесс бронирования (`book_car`)**:
   - Сотрудник запрашивает право забронировать автомобиль (`acquire()`).
   - После успешного резервирования сотрудник отпускает ресурс (`release()`), 
        чтобы следующий коллега мог воспользоваться сервисом.
3. **Сотрудники (потоки)**:
   - Каждый сотрудник представлен отдельным потоком, который пытается 
        зарезервировать автомобиль согласно установленным правилам.

---
### 🎯 **Преимущества использования семафоров в бизнесе**
- **Контроль доступа**: гарантия, что одновременно услугу могут 
    использовать только ограниченное количество сотрудников.
- **Избежание перегрузки**: снижение нагрузки на инфраструктуру 
    (серверы, базы данных) путём разумного распределения запросов.
- **Повышение качества обслуживания**: равномерное распределение 
    ресурсов между клиентами или сотрудниками.

---
### 📊 **Итог**
Семафоры — это отличный инструмент для контроля доступа к ресурсам в 
условиях конкуренции и ограничений. Особенно полезно это в сфере услуг, 
арендных сервисов и распределённых системах, где важно обеспечивать 
баланс и безопасность использования ресурсов.

Надеюсь, теперь концепция семафоров стала ещё яснее и практичнее!
'''