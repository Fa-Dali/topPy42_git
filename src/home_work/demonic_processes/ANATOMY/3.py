'''
Класс `Timer` из модуля `threading` — это специальная разновидность
потоков, предназначенная для отложенного выполнения задач спустя
определенное время. Подобно будильнику, он запускает определенную
функцию через установленный интервал времени.

### Как это устроено?
1. **`interval`** — временной промежуток (в секундах), через который
                    будет вызвана функция.
2. **`function`** — функция, которая будет выполнена после указанного
                    промежутка времени.
3. **`args`** — аргументы, которые передаются в функцию.
4. **`kwargs`** — именованные аргументы, которые также передаются в функцию.

### Подробности работы конструктора `Timer`:
- **Создание таймера**: Когда создаёшь объект `Timer`, задаёшь нужное
                    время задержки и функцию, которая должна быть вызвана
                    по истечении этого времени.
- **Запуск таймера**: Используется метод `start()`, чтобы запустить обратный
                    отсчет и выполнение функции.
- **Отмена таймера**: Таймер можно отменить до срабатывания
                    с помощью метода `cancel()`.

### Пример использования класса `Timer`:
Предположим, ты хочешь напомнить другу о дне рождения через 5 минут,
отправив SMS-сообщение. Вот как это можно сделать с помощью таймера:
'''

import threading
import time

# Функция, которая будет выполнена после истечения времени
def remind_birthday(name):
    print(f"🎉 С днём рождения, {name}!")

# Создаём таймер, который напомнит через 5 минут (300 секунд)
timer = threading.Timer(300, remind_birthday, args=["Петя"])

# Запускаем таймер
timer.start()

# Временно засыпаем основную программу, чтобы показать, что таймер
# работает параллельно
print("Таймер запущен, ожидайте напоминания через 5 минут.")
time.sleep(600)  # Ждём дольше, чтобы убедиться, что таймер сработал
'''
---
### Важные аспекты:
- **`interval`**: Значение в секундах, через которое сработает таймер.
- **`function`**: Это та функция, которая будет вызвана по окончании таймера.
- **`args` и `kwargs`**: Эти аргументы передаются в вашу функцию, чтобы она 
                        могла получить нужную информацию (например, имя друга, 
                        которому нужно пожелать счастья).
---
### Пример с именованным аргументом (`kwargs`):

Допустим, ты хочешь поздравить друга с подарком, но подарок нужно 
выбирать в зависимости от настроения друга. Используем именованные 
аргументы для передачи подарка:

'''
import threading
import time

# Функция поздравления с указанием подаренного предмета
def celebrate_bday(name, gift):
    print(f"🎉 С днем рождения, {name}! Вам дарят {gift}.")

# Создаём таймер с именованным аргументом
timer = threading.Timer(300,
                        celebrate_bday,
                        args=["Катя"],
                        kwargs={"gift": "шоколад"})

# Запускаем таймер
timer.start()

# Спим, чтобы продемонстрировать параллельность работы
print("Таймер запущен, ожидаем поздравления через 5 минут.")
time.sleep(600)  # Ожидай 10 минут, чтобы убедиться, что таймер сработал
'''
---
### Заключение:
Класс `Timer` предоставляет удобные возможности для отложенного 
выполнения задач, будь то напоминания, периодические оповещения 
или запланированные действия. Используя этот инструмент грамотно, 
можно значительно повысить эффективность программ, работающих с 
временем и таймерами.
'''
# ======================================================================
'''
Класс `Timer` из модуля `threading` позволяет выполнять функцию через 
определенный промежуток времени, подобно механизму таймера или будильника. 
Однако, помимо самого тайминга, существуют полезные методы, позволяющие 
управлять поведением таймера.

### Методы класса `Timer`:
1. #### **`start()`**:
   - **Описание**: Запускает таймер и инициирует обратный отсчет до активации 
                    функции.
   - **Применение**: Необходимо вызывать метод `start()`, чтобы таймер начал 
                    отсчитывать заданный интервал времени. До вызова этого 
                    метода таймер находится в состоянии покоя.
   - **Особенности**: После старта таймер нельзя перезапустить, если он уже 
                    отработал или был отменён.

   Пример использования:
'''
def my_function():
    pass

timer = threading.Timer(5, my_function)
timer.start()  # Начинает отсчет времени
'''

2. #### **`cancel()`**:
   - **Описание**: Останавливает запущенный таймер, если он ещё не успел 
                    сработать.
   - **Применение**: Используется, когда необходимо прервать работу таймера 
                    до того, как наступит срок выполнения функции.
   - **Особенности**: Если таймер уже выполнил свою задачу (функция уже 
                    вызвала), то метод `cancel()` не оказывает влияния.

   Пример использования:
'''
timer = threading.Timer(5, my_function)
timer.start()  # Запускаем таймер
# Предположим, обстоятельства изменились, и таймер нужно остановить
timer.cancel()  # Останавливаем таймер, если он ещё не завершился
'''
---
### Практический пример использования обоих методов:

Допустим, мы делаем маленькое приложение-напоминание для учёбы, которое 
предупреждает пользователя через пять минут о необходимости отдохнуть:

'''
import threading
import time

# Функция, которая будет выполняться по истечении таймера
def take_break():
    print("🛋️ Пришло время отдохнуть! Сделайте перерыв.")

# Создаём таймер, который сработает через 5 минут (300 секунд)
study_timer = threading.Timer(300, take_break)

# Запускаем таймер
study_timer.start()

# Некоторое время работаем и вдруг понимаем, что уже сделали достаточно отдыха
print("Уже отдохнули досрочно?")
response = input("Хотите отменить таймер? (yes/no): ")

if response.lower() == "yes":
    study_timer.cancel()  # Останавливаем таймер, если он ещё не сработал
    print("Таймер отмены успешно выполнен.")

# Ждём немного, чтобы увидеть эффект
time.sleep(600)  # Оставляем программу открытой на 10 минут, чтобы убедиться,
                # что таймер не сработал
'''
---
### Подробности работы таймера:
- **`start()`**: Запускает внутренний отсчёт времени. После истечения 
                установленного интервала таймер вызывает функцию, переданную 
                при его создании.
- **`cancel()`**: Позволяет прекратить отсчёт, если необходимо срочно отменить 
                задачу. Если таймер уже сработал, метод `cancel()` не 
                повлияет на его работу.

### Важные особенности:
- Оба метода (`start()` и `cancel()`) влияют на работу таймера только до 
        момента его срабатывания. Если таймер уже исполнил свою функцию, 
        повлиять на него уже нельзя.
- Несмотря на простоту, таймеры оказываются полезными инструментами для 
        автоматизации задач, связанных с таймингом, особенно когда нужно 
        отсрочить выполнение некоторых действий.
---
### Итог:
- **`start()`** запускает таймер и отсчёт времени до выполнения функции.
- **`cancel()`** останавливает запущенный таймер, если это необходимо до 
        его завершения.

Это универсальные инструменты для управления отложенными операциями в 
        ваших приложениях.
'''