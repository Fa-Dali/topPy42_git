# ======== 2 ========
'''
Вот подробный и наглядный пример использования всех перечисленных
# методов
класса `Thread`, адаптированный специально для объяснения детям и
начинающих программистов.

### История приключений маленького воробья 🐦
Маленький воробушек очень любит приключения, и однажды утром он отправился
исследовать мир вокруг своего гнезда. Пока воробышек путешествует, он
встречает разных друзей, и каждый из них делится интересным опытом и
знаниями. Наша задача — научить воробушка всему, что умеют потоки
(threads) в Python.
---

### Импорт необходимых инструментов:
'''
import threading
import time
'''
---
### Рассказ про приключение воробья:
'''
# Приключение начинается!
def adventure(vorobey_name):
    print(f"\n🚀 Воробей {vorobey_name} полетел навстречу новым друзьям!")
    time.sleep(2)  # Пауза на 2 секунды
    print(f"🍂 Воробей встретил белочку и узнал, как собирать орехи.")
    time.sleep(3)  # Еще пауза на 3 секунды
    print(f"🎯 Потом он нашел ёжика и научился кататься на велосипеде!")
    time.sleep(4)  # Продолжаем отдых на 4 секунды
    print(f"🥺 Наконец, воробей познакомился с лягушкой "
          f"и прыгнул с неё высоко-высоко!")
    time.sleep(5)  # Ещё одна пауза на 5 секунд
    print(f"🐦 Воробышек вернулся домой счастливым и отдохнувшим!")
'''
---
### Применение методов класса `Thread`:
'''
# Глава 1: Создание путешествия воробья
travel_thread = threading.Thread(target=adventure,
                                 args=("Кешик",),
                                 name="Путешествие воробья")

# Глава 2: Начало путешествия
travel_thread.start()  # Метод start() запускает путешествие воробья
print("⭐️ Воробей Кешик улетел искать приключения!")

# Глава 3: Узнать, активный ли поток
if travel_thread.is_alive():
    print("✈️ Путешествие продолжается!")
else:
    print("☁️ Воробей уже вернулся домой.")

# Глава 4: Взгляд на идентификатор путешествия
print(f"🆕 Идентификатор путешествия: {travel_thread.ident}")

# Глава 5: Проверка статуса демона
print(f"🪄 Является ли путешествие демоном: {travel_thread.isDaemon()}")

# Глава 6: Ожидание возвращения воробья
travel_thread.join()  # Метод join() ждёт, пока воробей вернется домой
print("🏵️ Воробей Кешик благополучно прибыл домой!")

# Глава 7: Задание имени воробью
new_name = "Шустрик"
travel_thread.setName(new_name)
print(f"🌟 Новое имя воробья: {travel_thread.getName()}")

# Глава 8: Изменение статуса демона
travel_thread.setDaemon(True)
print(f"🌬️ Теперь путешествие воробья - демон: {travel_thread.isDaemon()}")
'''
---
### Полноценный итоговый пример:
'''
import threading
import time

# Приключение начинается!
def adventure(vorobey_name):
    print(f"\n🚀 Воробей {vorobey_name} полетел навстречу новым друзьям!")
    time.sleep(2)  # Пауза на 2 секунды
    print(f"🍂 Воробей встретил белочку и узнал, как собирать орехи.")
    time.sleep(3)  # Еще пауза на 3 секунды
    print(f"🎯 Потом он нашел ёжика и научился кататься на велосипеде!")
    time.sleep(4)  # Продолжаем отдых на 4 секунды
    print(f"🥺 Наконец, воробей познакомился с лягушкой и прыгнул с неё "
          f"высоко-высоко!")
    time.sleep(5)  # Ещё одна пауза на 5 секунд
    print(f"🐦 Воробышек вернулся домой счастливым и отдохнувшим!")

# Глава 1: Создание путешествия воробья
travel_thread = threading.Thread(target=adventure,
                                 args=("Кешик",),
                                 name="Путешествие воробья")

# Глава 2: Начало путешествия
travel_thread.start()  # Метод start() запускает путешествие воробья
print("⭐️ Воробей Кешик улетел искать приключения!")

# Глава 3: Узнать, активный ли поток
if travel_thread.is_alive():
    print("✈️ Путешествие продолжается!")
else:
    print("☁️ Воробей уже вернулся домой.")

# Глава 4: Взгляд на идентификатор путешествия
print(f"🆕 Идентификатор путешествия: {travel_thread.ident}")

# Глава 5: Проверка статуса демона
print(f"🪄 Является ли путешествие демоном: {travel_thread.isDaemon()}")

# Глава 6: Ожидание возвращения воробья
travel_thread.join()  # Метод join() ждёт, пока воробей вернется домой
print("🏵️ Воробей Кешик благополучно прибыл домой!")

# Глава 7: Задание имени воробью
new_name = "Шустрик"
travel_thread.setName(new_name)
print(f"🌟 Новое имя воробья: {travel_thread.getName()}")

# Глава 8: Изменение статуса демона
travel_thread.setDaemon(True)
print(f"🌬️ Теперь путешествие воробья - демон: {travel_thread.isDaemon()}")
'''
---
### Что произойдёт при запуске:
1. **Приключение начинается**: воробей Кешик отправляется в путешествие, 
                                встречая интересных друзей.
2. **Проверка статуса**: посмотрим, активен ли поток и узнаем его 
                        идентификатор.
3. **Ожидание**: с помощью метода `join()` мы ожидаем возвращения воробья 
                домой.
4. **Переименование**: воробей получает новое имя.
5. **Становится демоном**: изменим статус путешествия, сделав его демоническим.
---
### Рекомендации для дальнейшего изучения:
- Попробуй поменять продолжительность пауз (`time.sleep()`) и увидеть, как 
    изменится последовательность событий.
- Экспериментируй с созданием большего количества потоков и попробуй провести 
    их параллельно.
- Задавай себе интересные задачи и смотри, как потоки помогают их решать.

Желаем веселых путешествий и увлекательных экспериментов!
'''
#======================================================================
'''
ПОЧЕМУ НЕ ПРИМЕНЯЕТСЯ __init__

Очень хороший вопрос! Спасибо, что заметил. Дело в том, что методы, 
перечисленные в описании конструктора `Thread`, относятся к встроенному 
классу `Thread` из модуля `threading`. Этот класс уже реализован системой 
Python, и для большинства простых случаев использования потоков его хватает 
без дополнительной модификации через собственный конструктор (`__init__`).

Тем не менее, если хочется создать кастомный поток, наследующийся от 
базового класса `Thread`, и определить дополнительный конструктор 
(`__init__`), это тоже возможно. В таком случае в собственном классе 
нужно вызвать конструктор базового класса, чтобы обеспечить полную 
работоспособность потока.

### Зачем нужен `__init__` в подклассе `Thread`?

Если тебе нужно расширить функциональность класса `Thread`, добавить 
собственные свойства или произвести предварительные настройки перед 
началом выполнения потока, то можно использовать конструктор (`__init__`).

Пример такого расширения покажу ниже.
---
### Пример с собственным конструктором (`__init__`)

Допустим, мы хотим создать подкласс `Thread`, в котором будем запоминать 
дополнительное свойство — местоположение нашего воробья (`location`):

'''
import threading
import time

class AdventureThread(threading.Thread):
    def __init__(self, vorobey_name, location):
        super().__init__()  # Обязательно вызываем конструктор базового класса
        self.vorobey_name = vorobey_name
        self.location = location  # Наше расширение класса

    def run(self):
        print(f"\n🚀 Воробей {self.vorobey_name} полетел навстречу "
              f"новым друзьям в {self.location}!")
        time.sleep(2)  # Пауза на 2 секунды
        print(f"🍂 Воробей встретил белочку и узнал, как собирать орехи.")
        time.sleep(3)  # Еще пауза на 3 секунды
        print(f"🎯 Потом он нашел ёжика и научился кататься на велосипеде!")
        time.sleep(4)  # Продолжаем отдых на 4 секунды
        print(f"🥺 Наконец, воробей познакомился с лягушкой и прыгнул "
              f"с неё высоко-высоко!")
        time.sleep(5)  # Ещё одна пауза на 5 секунд
        print(f"🐦 Воробышек вернулся домой счастливым и отдохнувшим!")

# Создаем и запускаем путешествие воробья
travel_thread = AdventureThread("Кешик", "лес")
travel_thread.start()

# Ждем завершения путешествия
travel_thread.join()

print("🏵️ Воробей Кешик благополучно прибыл домой!")
'''

### Что происходит в этом примере?
- Мы создаем подкласс `AdventureThread`, расширяя базовый класс `Thread`.
- В конструкторе (`__init__`) мы вызываем конструктор базового класса с 
        помощью `super().__init__()`, чтобы поток работал корректно.
- Добавляем собственное свойство `location`, которое хранится в экземпляре 
        потока.
- Переопределённый метод `run()` теперь уже выполняет нашу собственную 
        логику приключений.
---
### Почему изначально я не использовал `__init__`?
Обычно необходимость в создании собственного конструктора возникает, 
когда нужно:
- Добавить специальные свойства (например, координаты, параметры или данные, 
        важные для потока).
- Произвести предварительные расчеты или настройку перед началом выполнения 
        потока.

Если таких потребностей нет, то прямой вызов конструктора базового класса не 
обязателен. Достаточно воспользоваться готовым методом `Thread`, установив 
нужные аргументы в виде `target`, `args`, `kwargs` и прочих свойств.
---
### Итог:
- Без использования собственного конструктора (`__init__`) можно обойтись, 
        если базовые возможности класса `Thread` покрывают потребности.
- Если нужна собственная логика инициализации или хранение уникальных данных 
        для потока, то можно расширить класс и добавить нужный конструктор, 
        вызывая базовую реализацию через `super().__init__()`.

Надеюсь, теперь картина стала яснее!
'''
#======================================================================
'''
Давай увеличим масштаб нашей истории про воробья, добавив больше возможностей 
и углубляясь в понимание классов и методов Python. При этом постараемся 
охватить большее количество аспектов работы с потоками и унаследованными 
классами.

### Широкий пример с воробьем и дополнительным контентом:
Представь, что маленький воробей Кешик не просто путешественник, 
а настоящий исследователь мира природы. Помимо прогулок и приключений, 
он участвует в важных научных экспедициях и исследованиях природных 
явлений.
---
### Нам предстоит реализовать:
- Многократные экспедиции воробья в разные места.
- Собирать наблюдения и делиться ими с сообществом ученых-птичек.
- Использовать потоки, чтобы воробьи исследовали природу одновременно.
- Применить наследование и собственные конструкторы для гибкости и удобства.
---
### Подробный пример:
'''
import threading
import random
import time

# Класc ExplodingVoroBey — наследник класса Thread
class ExploringVorobej(threading.Thread):
    def __init__(self, name, locations):
        """
        :param name: Имя воробья-исследователя
        :param locations: Список точек исследования
        """
        super().__init__()  # Вызываем конструктор базового класса
        self.name = name
        self.locations = locations  # Местоположения экспедиций
        self.observations = []  # Наблюдения воробья

    def explore_location(self, location):
        """
        Исследует местность и записывает наблюдение.
        """
        print(f"\n🛠️ Воробей {self.name} проводит исследование "
              f"местности '{location}'")
        observation = (f"Воронки размером {random.randint(1, 5)} см "
                       f"обнаружены.")
        self.observations.append(observation)
        time.sleep(random.uniform(1, 3))  # Проводим исследование
                                                # (случайное время)
        print(f"📝 Исследование в '{location}' завершено!")

    def share_observations(self):
        """
        Делится собранными наблюдениями.
        """
        print(f"\n📣 Воробей {self.name} делится наблюдениями:")
        for obs in self.observations:
            print(obs)

    def run(self):
        """
        Основной метод, который выполняет воробей-исследователь.
        """
        for loc in self.locations:
            self.explore_location(loc)
        self.share_observations()

# Список мест исследований
locations = ["Северный лес", "Южный океан", "Восточная пустыня"]

# Создаем группу исследователей
research_team = [
    ExploringVorobej("Кешик", locations),
    ExploringVorobej("Веснушка", locations),
    ExploringVorobej("Пухлик", locations)
]

# Запускаем экспедиции одновременно
for researcher in research_team:
    researcher.start()

# Ждем завершения всех экспедиций
for researcher in research_team:
    researcher.join()

print("\n🗞️ Научная конференция завершилась! Результаты опубликованы.")
'''
---
### Что здесь происходит?
1. **Класс `ExploringVorobej`**:
   - Наследуется от класса `Thread`, чтобы представлять 
        воробьев-исследователей.
   - Имеет конструктор (`__init__`), где устанавливается имя воробья и 
        список точек исследования.
   - Включает метод `explore_location`, который имитирует проведение 
        научного исследования в конкретной точке.
   - Хранит собранные наблюдения в списке `observations`.
   - Метод `share_observations` публикует результаты исследований.

2. **Многопоточность**:
   - Создаются несколько воробьёв-исследователей, каждый из которых 
        запускается в своём потоке.
   - Каждый воробей параллельно исследует различные точки, симулируя 
        реальные научные изыскания.

3. **Совместная работа**:
   - Все исследователи проводят свои исследования одновременно, но 
        публикуют результаты только после завершения всей научной 
                конференции.
---
### Выводы:
- **Наследование и собственные конструкторы**: с помощью `__init__` мы 
        смогли расширить класс `Thread`, добавив полезную функциональность 
        (например, список посещаемых мест и сбор наблюдений).
- **Потоки** помогли модели реальности, где несколько учёных одновременно 
        занимаются важными делами.
- **Параллельные процессы**: продемонстрировали, как потоки работают 
        совместно, ускоряя общую производительность.

Теперь, вооружившись этим широким примером, можно применять потоки и 
наследование в реальных проектах и развивать творческие проекты на Python!
'''