'''
=== 5 ===
Давайте разбираться подробно и доступным языком!

## 🌸 Что такое блокировки (locks)?

Когда несколько потоков пытаются одновременно изменять или использовать
общие ресурсы (например, счетчик, массив данных или файл), это может
привести к проблемам, связанным с несогласованной работой (race conditions).
Представь, что двое детей играют с одной игрушкой одновременно — каждый
тянет её в свою сторону, и игрушка ломается. Примерно так же ведут себя
потоки, когда хотят одновременно изменить один и тот же ресурс.

Чтобы защитить ресурс от хаоса, мы применяем блокираторы (locks),
которые создают подобие замка: если один поток "заблокировал" ресурс,
никто другой не сможет его трогать, пока первая операция не завершится.

---

## 📍 Механизм блокировки (`Lock`):

В Python блокировщики представлены классом `Lock` из модуля `threading`.
Когда поток хочет использовать общий ресурс, он "берёт замок"
(метод `acquire()`) и освобождает его, когда работа завершена
(метод `release()`).

### Типичный шаблон использования:

'''
lock = threading.Lock()

# Критическая зона (защищаемый участок кода)
lock.acquire()  # Берём замок
try:
    # Производим изменения общих данных
finally:
    lock.release()  # Освобождаем замок
'''

---
## 🔹 Пример использования `Lock`:
Представь, что ты пишешь счётчик посетителей сайта, и несколько потоков 
хотят увеличить его одновременно. Вот как мы защитим ресурс от разногласий:

'''
import threading

# Общий счётчик посетителей
visitor_counter = 0

# Замок для защиты счётчика
counter_lock = threading.Lock()

def increase_visitors(count):
    global visitor_counter
    counter_lock.acquire()  # Заблокировать ресурс
    try:
        visitor_counter += count
        print(f"Текущий счётчик: {visitor_counter}")
    finally:
        counter_lock.release()  # Освободить ресурс

# Запускаем потоки, увеличивающие счётчик
threads = []
for i in range(5):
    thread = threading.Thread(target=increase_visitors, args=(1,))
    threads.append(thread)
    thread.start()

# Ждём завершения всех потоков
for thread in threads:
    thread.join()

print(f"Конечное значение счётчика: {visitor_counter}")
'''

### Что здесь происходит?
- Создали замок (`counter_lock`), защищающий доступ к общему ресурсу 
    (счетчику посетителей).
- Каждый поток берет замок перед изменением счётчика, а затем освобождает его.
- В итоге, несмотря на конкуренцию, счётчик увеличивается корректно.

---
## 🔄 Что случится, если не использовать блокировки?
Без замков (lock) потоки могут пытаться одновременно увеличивать счётчик, 
что приведет к непредсказуемым результатам. Например, представь, что один 
поток увеличивает счётчик от 10 до 11, но в этот момент другой поток 
прочитал старое значение (10) и добавил единицу, получив опять 11. 
В итоге счётчик окажется меньше, чем должен быть.

---
## 🚧 Заключение:
Блокировки (`Lock`) помогают избежать конфликтов и ошибок при совместной 
работе потоков. Их применяют везде, где важна целостность данных и 
порядок выполнения операций. Важно помнить, что замки надо беречь 
бережно и всегда освобождать, чтобы не заблокировать поток навсегда.

---
Теперь ты знаешь, как защищать свои ресурсы от конкурентов и добиваться 
стабильности в многопоточности!

=======================================================================

Давай представим это так, чтобы было понятно даже малышу. 
Вспомним наше волшебное королевство пчелок и познакомимся с забавной 
историей о важности замочков для пчелок!

### 🐝 История про маленьких пчелок и сладкий горшочек мёда

Представь, что в чудесном саду живёт дружная семья пчелок. У них есть 
большой сладкий горшочек меда, и все пчелки любят заглядывать туда, 
чтобы подкрепиться медом.

Однажды случилось неприятное происшествие: оказалось, что мед пропадает 
быстрее обычного! Оказалось, что многие пчелки одновременно лезут в 
горшочек, переливая друг друга и разбрызгивая драгоценный продукт.

Королеву пчелок это совсем не устраивает, и она придумала гениальное 
решение: **замочки**! Теперь, если пчёлка хочет набрать меда, она берёт 
волшебный замочек, достаёт свою долю, а затем возвращает замочек назад.

---
### 🖥️ **Реализация детского примера на Python**
Наш код покажет, как пчелки используют замочки, чтобы избежать 
неразберихи и разделить запасы меда справедливо.

'''
import threading
import time
import random

# 📌 Горшочек меда (наш общий ресурс)
honey_jar = {"med": 100}  # Изначально 100 капелек меда

# 🔐 Волшебный замочек для защиты горшка меда
jar_lock = threading.Lock()

# 🐝 Отдельная функция для каждой пчелки
def eat_honey(beename):
    global honey_jar
    jar_lock.acquire()  # Беру замочек
    try:
        # Смотрю, сколько меда осталось
        current_med = honey_jar["med"]
        if current_med >= 10:
            taken_med = random.randint(5, 10)  # Беру от 5 до 10 капелек
            honey_jar["med"] -= taken_med
            print(f"🐝 Пчёлка {beename} взяла {taken_med} капелек меда. Осталось {honey_jar['med']} капелек.")
        else:
            print(f"🐝 Пчёлка {beename} увидела пустой горшочек и грустно вернулась.")
    finally:
        jar_lock.release()  # Возвращаю замочек

# 🐝 Заводим несколько пчелок
threads = []
for beename in ["Поля", "Люсечка", "Маринка"]:
    thread = threading.Thread(target=eat_honey, args=(beename,))
    threads.append(thread)
    thread.start()

# 🏡 Ждём, пока пчелки поедят
for thread in threads:
    thread.join()

# 🍯 Показываем, сколько меда осталось
print(f"Осталось меда в горшочке: {honey_jar['med']} капелек.")
'''

---
### 🌱 **Что происходит в этом коде?**
1. **Горшочек меда (`honey_jar`)**:
   - Это наш общий ресурс, который охраняется волшебным замочком.
2. **Замочек (`jar_lock`)**:
   - Он защищает горшочек меда, гарантируя, что только одна пчелка 
       может брать мед одновременно.
3. **Пчёлки (`eat_honey`)**:
   - Каждая пчелка подходит к горшочку и берет волшебный замочек.
   - Она смотрит, сколько меда осталось, берет себе немного и возвращает 
       замочек.
4. **Работа потоков**:
   - Мы запускаем несколько пчелок одновременно, чтобы показать, как они 
        совместно пользуются общим ресурсом.

---
### 🐝 **Заключение для малышей**
Волшебные замочки — это полезный инструмент, чтобы поделить ресурсы 
честно и избежать споров. Они защищают сокровища от разграбления и 
позволяют соседям жить в мире и гармонии.

Научившись пользоваться замочками, ты станешь настоящим мастером порядка 
и справедливости в компьютерных программах!

=======================================================================

Давай рассмотрим бизнес-пример, который наглядно продемонстрирует 
применение блокировок и синхронизации потоков на практике.

Представь компанию, которая производит высокотехнологичные гаджеты, и 
одновременно обрабатывает заказы покупателей. Есть центральный склад, 
где хранятся товары, и множество сотрудников-продавцов, которые 
одновременно размещают заказы клиентов. Возникает опасность, что 
несколько продавцов захотят забрать товар одновременно, и возможна 
ситуация, когда один заказ продублирован или товар вообще исчезнет.

Для предотвращения подобной проблемы мы воспользуемся блокировкой 
(`Lock`), чтобы обезопасить доступ к центральному складу.

---
### 📌 Бизнес-пример: магазин электроники с защитой от дублирования заказов

'''
import threading
import time
import random

# Склад товаров (общий ресурс)
inventory = {"iphone": 10, "samsung": 15, "xiaomi": 20}

# 🔐 Замок для защиты склада
inventory_lock = threading.Lock()

# Продажа товара продавцом
def sell_product(seller_name, product, quantity):
    inventory_lock.acquire()  # Заблокировать доступ к инвентарю
    try:
        # Проверяем наличие товара
        if inventory[product] >= quantity:
            inventory[product] -= quantity
            print(f"Продавец {seller_name} продал {quantity} шт. {product}. Осталось {inventory[product]} штук.")
        else:
            print(f"Продавец {seller_name} не смог продать {product}, недостаточно запасов.")
    finally:
        inventory_lock.release()  # Освободить доступ к инвентарю

# Запускаем продажи
threads = []
products = ["iphone", "samsung", "xiaomi"]

for seller in ["Иван", "Анна", "Максим"]:
    product = random.choice(products)
    quantity = random.randint(1, 5)
    thread = threading.Thread(target=sell_product, args=(seller, product, quantity))
    threads.append(thread)
    thread.start()

# Ждём завершения продаж
for thread in threads:
    thread.join()

# Показываем остатки на складе
print("\n📦 Остатки на складе:")
for item, stock in inventory.items():
    print(f"{item.capitalize()} - {stock} шт.")
'''

---
### 📈 Что здесь происходит?
1. **Склад (`inventory`)**:
   - Центральный склад, содержащий доступные товары (например, смартфоны).
2. **Замок (`inventory_lock`)**:
   - Замок гарантирует, что продавец не может продавать товар одновременно 
        с другим сотрудником.

3. **Продажа продукта (`sell_product`)**:
   - Каждый продавец пытается продать товар покупателю, предварительно 
        проверив доступность товара.
   - Если товара достаточно, он уменьшает количество на складе.

4. **Несколько потоков (продавцы)**:
   - Продавцы работают одновременно, и благодаря замку продажи проходят 
        гладко и без дублирования заказов.

---
### 💰 Бизнес-значение:
Благодаря такому решению компания предотвращает:
- **Дублирование заказов**: когда два продавца продают один и тот 
        же товар одновременно.
- **Перепродажу**: когда товар оказывается проданным дважды.
- **Автоматизацию**: продажи становятся надежными и управляемыми, повышая 
        доверие клиентов и снижая издержки.

---
### 💡 Заключение:
Этот пример демонстрирует реальную бизнес-проблему и её элегантное решение 
с помощью блокировок и синхронизации потоков. Подобные подходы широко 
применяются в электронной коммерции, банковских операциях и логистике 
для защиты ценных ресурсов и повышения эффективности бизнеса.
'''
