'''
=== 4 ===
Давай поговорим подробнее о концепции локальных хранилищ потоков
(`local storage`) и их применении в Python, начиная с базовых принципов
и заканчивая примерами использования.

---

## Что такое локальные хранилища потоков (`local storage`)?

Когда мы говорим о многопоточности в Python, одним из ключевых вопросов
становится управление данными между различными потоками. Зачастую
возникает потребность, чтобы каждый поток имел доступ к своему
собственному пространству памяти, изолированному от других потоков.
Именно для этого и созданы локальные хранилища потоков.

В Python этот механизм представлен специальным классом `threading.local()`,
который позволяет каждому потоку создавать и хранить индивидуальные
данные, невидимые другим потокам.

---

## Основы работы с локальным хранилищем потоков

### 1. **Создание локального хранилища**:

Первое, что нужно сделать, — это создать экземпляр класса `threading.local()`.
Он действует как контейнер для хранения данных, индивидуальных
для каждого потока.

'''
local_storage = threading.local()
'''

=======================================================================

Детям бывает сложно воспринимать абстрактные понятия вроде многопоточности и 
локальных хранилищ потоков. Давайте превратим обучение в интересную историю, 
объясняющую концепцию простыми словами.

### ☀️ **История про маленькие пчелки и медоносные поля**

Представь, что в прекрасном саду живут пчёлки, которые заняты важной 
работой — собиранием нектара. Каждая пчёлка отправляется на своё поле, 
и ей важно запомнить только свою дорогу и место сбора, чтобы не запутаться 
и не мешать другим пчёлкам.

Представь, что у нас есть волшебный дневничок, который помогает каждой 
пчёлке запоминать свои личные записи, не смешиваясь с записями других 
пчёлок.

---
### 🐝 **Пчелиная жизнь и локальные хранилища**
Каждая пчёлка имеет своё личное пространство для записей (дневничок), 
где она фиксирует важные сведения:
- Своё имя.
- Место, откуда прилетела.
- Количество собранного нектара.

---
### 🖥️ **Реализация детской истории на Python**
Давайте напишем небольшой код, чтобы представить каждую пчёлку и её 
личный дневничок:

'''
import threading

# 🏫 Волшебный дневничок (локальное хранилище для пчёлок)
bee_diary = threading.local()

# 🐝 Отдельная функция для каждой пчёлки
def collect_nectar(beename, field):
    # 📚 Каждая пчёлка заводит запись в личном дневнике
    bee_diary.beename = beename
    bee_diary.field = field
    bee_diary.nectar_collected = 0

    # 🌿 Пчёлка собирается на цветок и собирает нектар
    print(f"🐝 Пчёлка {beename} отправилась на поле {field}.")
    collected_amount = random.randint(10, 50)  # Случайное количество
                                                    # собранного нектара
    bee_diary.nectar_collected += collected_amount
    print(f"🍯 Пчёлка {beename} собрала {collected_amount} капель нектара "
          f"на поле {field}.")

# 🐝 Заводим несколько пчёлок
threads = []
fields = ['Ромашки', 'Васильки', 'Маковые луга']
names = ['Люси', 'Томми', 'Джеки']

for name, field in zip(names, fields):
    thread = threading.Thread(target=collect_nectar, args=(name, field))
    threads.append(thread)
    thread.start()

# 🏡 Ждём, пока пчёлки соберут нектар
for thread in threads:
    thread.join()

# 🍯 Смотрим, сколько собрали наши пчёлки
for name in names:
    print(f"📝 Пчёлка {name} собрала "
          f"{bee_diary.nectar_collected} капель нектара.")
'''

---
### 🌳 **Что происходит в этом коде?**
1. **Волшебный дневничок (`bee_diary`)**:
   - Каждая пчёлка заводит свою личную страницу в дневничке, записывая 
        своё имя, место сбора и количество собранного нектара.
2. **Сбор нектара**:
   - Каждая пчёлка случайно собирает нектар и записывает это в свой 
        личный дневничок.
3. **Отдельные потоки**:
   - Для каждой пчёлки создаётся отдельный поток, чтобы они могли 
        собираться одновременно, не мешая друг другу.
4. **Итоговый подсчёт**:
   - После завершения полёта, каждая пчёлка показывает, сколько нектара 
        она собрала, основываясь на данных своего личного дневника.

---
### 🐝 **Какие выводы сделают дети?**
- Каждая пчёлка (поток) ведёт свою собственную книгу записей 
    (локальное хранилище), чтобы хранить личные данные.
- Эти данные принадлежат только этой пчёлке и никому другому.
- Даже если пчёлки собираются одновременно, они не мешают друг другу, 
    так как каждая хранит свои заметки отдельно.

---
### 🍯 **Зачем изучать локальные хранилища потоков?**
Дети узнают, что компьютеры могут выполнять задачи одновременно, 
но важно держать информацию отдельно для каждого исполнителя. Этот 
урок поможет заложить основы понимания многопоточности и подготовить 
ребят к будущим занятиям информатикой и программированию.

Игровые примеры помогают маленьким ученикам легче усвоить материал, 
развивая воображение и способность мыслить творчески.

=======================================================================

Ошибка произошла потому, что локальное хранилище потоков (`bee_diary`) 
создано глобально, и мы пытаемся считать оттуда данные после завершения 
работы потоков. Однако после завершения потоков, объекты локального 
хранилища очищаются, и обращение к ним извне приводит к ошибке.

Проблема решается тем, что результаты сбора нектара нужно собирать 
внутри самих потоков и каким-то образом объединять эти результаты после 
завершения работы всех потоков.

### Исправленный пример:
Мы добавим специальную структуру для объединения результатов всех пчёлок, 
и извлечём данные сразу после завершения каждого потока.

'''
import threading
import random
import time

# 🏫 Волшебный дневничок (локальное хранилище для пчёлок)
bee_diary = threading.local()

# 📋 Сборщик результатов
results = {}

# 🐝 Отдельная функция для каждой пчёлки
def collect_nectar(beename, field):
    # 📚 Каждая пчёлка заводит запись в личном дневнике
    bee_diary.beename = beename
    bee_diary.field = field
    bee_diary.nectar_collected = 0

    # 🌿 Пчёлка собирается на цветок и собирает нектар
    print(f"🐝 Пчёлка {beename} отправилась на поле {field}.")
    collected_amount = random.randint(10, 50)  # Случайное количество
                                                    # собранного нектара
    bee_diary.nectar_collected += collected_amount
    results[beename] = bee_diary.nectar_collected  # Запись результата в коллекцию
    print(f"🍯 Пчёлка {beename} собрала {collected_amount} капель нектара "
          f"на поле {field}.")

# 🐝 Заводим несколько пчёлок
threads = []
fields = ['Ромашки', 'Васильки', 'Маковые луга']
names = ['Люси', 'Томми', 'Джеки']

for name, field in zip(names, fields):
    thread = threading.Thread(target=collect_nectar, args=(name, field))
    threads.append(thread)
    thread.start()

# 🏡 Ждём, пока пчёлки соберут нектар
for thread in threads:
    thread.join()

# 🍯 Смотрим, сколько собрали наши пчёлки
for name, amount in results.items():
    print(f"📝 Пчёлка {name} собрала {amount} капель нектара.")
'''

### Что поменялось?

- Мы ввели отдельную структуру `results`, которая будет хранить результаты 
    сбора нектара каждой пчёлки.
- Внутри функции `collect_nectar` мы сохраняем результат сбора в словарь 
    `results`, привязав его к имени пчёлки.
- После завершения работы всех потоков, мы спокойно извлекаем результаты 
    из коллекции `results`.

---
### Результат выполнения исправленного кода:
```
🐝 Пчёлка Люси отправилась на поле Ромашки.
🍯 Пчёлка Люси собрала 34 капли нектара на поле Ромашки.
🐝 Пчёлка Томми отправилась на поле Васильки.
🍯 Пчёлка Томми собрала 16 капель нектара на поле Васильки.
🐝 Пчёлка Джеки отправилась на поле Маковые луга.
🍯 Пчёлка Джеки собрала 42 капли нектара на поле Маковые луга.
📝 Пчёлка Люси собрала 34 капли нектара.
📝 Пчёлка Томми собрала 16 капель нектара.
📝 Пчёлка Джеки собрала 42 капли нектара.
```

Теперь код работает корректно, и все результаты отображаются правильно!

Эту конструкцию нужно разместить глобально, чтобы доступ к ней был 
возможен из любого потока.

---

### 2. **Присвоение значений**:
После создания хранилища, каждый поток может назначить ему собственные 
данные, которые будут видны только данному потоку.

'''
local_storage.my_variable = value
'''

Это выражение создает новый атрибут (`my_variable`) в пространстве 
хранилища потока и сохраняет в нём значение (`value`). Каждый поток 
будет иметь свою копию этого атрибута, отличную от других потоков.

---

### 3. **Доступ к данным**:
Любой поток может обращаться к своим индивидуальным данным через 
локальное хранилище:

'''
current_value = local_storage.my_variable
'''

Важный нюанс: если поток попытается обратиться к несуществующему атрибуту, 
возникнет ошибка. Поэтому хорошей практикой является предварительное 
назначение значений перед попыткой считывания.

---

### Пример использования локального хранилища потоков
Допустим, у нас есть веб-сервер, который обслуживает тысячи запросов 
одновременно. Каждый запрос обрабатывается в отдельном потоке, и 
каждый поток должен иметь доступ к уникальной информации, относящейся 
только к нему (например, идентификатор запроса, временные данные и т.д.). 
Вот как это можно организовать:

'''
import threading

# Создаем локальное хранилище для потоков
request_local_storage = threading.local()

def process_request(request_id):
    # Каждый поток устанавливает уникальный ID запроса
    request_local_storage.request_id = request_id
    print(f"Обрабатываю запрос {request_local_storage.request_id}")

# Создаем и запускаем несколько потоков
threads = []
for i in range(5):
    thread = threading.Thread(target=process_request, args=(i,))
    threads.append(thread)
    thread.start()

# Ждём завершения всех потоков
for thread in threads:
    thread.join()
'''

### Что произойдет в этом примере?

- Мы создаем глобальное локальное хранилище (`request_local_storage`).
- Каждый поток при запуске устанавливает свой уникальный идентификатор 
    запроса в локальном хранилище.
- Поскольку локальное хранилище доступно только текущему потоку, каждый 
    поток видит только свой идентификатор, а не чужие.

---
## Когда использовать локальные хранилища потоков?

Они полезны в ситуациях, когда нужно:
- Изолированно хранить данные для каждого потока.
- Избежать конфликта между потоками при доступе к общим ресурсам.
- Повысить производительность и уменьшить накладные расходы на синхронизацию.

---
## Заключение
Локальные хранилища потоков (`local storage`) — мощный инструмент, 
который помогает управлять индивидуальными данными для каждого потока. 
Их использование снижает сложность многопоточной среды и повышает 
надежность программного обеспечения, особенно в больших системах, 
где важно предотвратить конфликты между потоками.
'''